enum Option[a] = {
  Some(a),
  None,
}

fn unwrap_or(value: Option[Int], fallback: Int) -> Int = match value {
  case Option.Some(x): x
  case Option.None: fallback
}

puts(unwrap_or(Option.Some(42), 0))
puts(unwrap_or(Option.None, 7))
