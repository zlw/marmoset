trait show[a] {
    fn show(x: a) -> string
}

impl show for int {
    fn show(x: int) -> string {
        "" + x + ""
    }
}

impl show for bool {
    fn show(x: bool) -> string {
        "bool"
    }
}

puts(1.show())
