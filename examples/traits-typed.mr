shape Named = {
  name: Str
}

fn get_name[t: Named](value: t) -> Str = value.name

let person = { name: "alice", age: 42 }
let company = { name: "acme", employees: 10 }

puts(get_name(person))
puts(get_name(company))
