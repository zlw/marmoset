fn describe(value: Int | Str | Bool) -> Str = {
  if (value is Int) {
    "int"
  } else {
    if (value is Str) {
      "string"
    } else {
      "bool"
    }
  }
}

puts(describe(42))
puts(describe("marmoset"))
puts(describe(true))
