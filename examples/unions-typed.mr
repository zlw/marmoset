let describe = fn(value: int | string | bool) -> string {
  if (value is int) {
    "int"
  } else {
    if (value is string) {
      "string"
    } else {
      "bool"
    }
  }
}

puts(describe(42))
puts(describe("marmoset"))
puts(describe(true))
