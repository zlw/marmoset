# P0 Fix Plan

## Progress

- Last updated: 2026-02-27
- Completed in this branch:
  - Emitter type lookup now fails hard on missing type-map entries (no silent `TNull` fallback).
  - Specialized function emission no longer ignores inference failures; failures surface as structured codegen errors.
  - Specialization/type-flow for top-level function references is deterministic and no longer depends on ad-hoc re-inference gaps.
  - Deterministic compilation checks were added and stabilized for repeated builds of the same source.
- Still open (P0 class):
  - None.

## Scope

Critical correctness issues that currently produce invalid Go output:

- Specialized function emission re-infers bodies with incomplete environments.
- Inference failure during specialized emission is ignored.
- Missing type-map entries silently degrade to `TNull`, producing wrong mangled calls (for example `f_unit`).

## Concrete Work

1. Remove silent fallback in codegen type lookup.
- In `/Users/zlw/src/marmoset/marmoset-ml/lib/backend/go/emitter.ml`, replace `get_type` fallback-to-`TNull` with hard failure carrying expression id/context.
- If codegen needs a type and it is missing, fail early with a compiler error.

2. Stop re-inferencing specialized functions with prelude-only env.
- Build a function environment that includes all collected user function schemes before emitting any specialization.
- Pass that env into specialized body emission so inter-function calls typecheck consistently.

3. Handle inference failure explicitly during specialization.
- In `emit_specialized_func`, replace ignored result (`let _ = ...`) with explicit match.
- Surface a clear backend error if function-body inference fails.

4. Keep specialization deterministic and closed under references.
- Ensure specialization pass has stable ordering and deterministic output for equal inputs.
- Ensure specialization can resolve calls between specialized top-level functions.

## Regression Tests

1. Top-level function calling top-level function.
- Example:
  - `let f = fn(x: int) -> int { x }`
  - `let g = fn(y: int) -> int { f(y) }`
  - `puts(g(1))`
- Must build and run without `f_unit` or missing-type warnings.

2. No missing-type warnings in successful builds.
- Build should never print `Warning: No type found for expression id ...` on passing programs.

3. Failure path quality.
- Inject a known unsupported construct in specialized function body and verify compiler emits a deterministic, actionable error message (not malformed Go).

## Exit Criteria

- No silent type fallback in emitter paths.
- Cross-function specialized calls compile and run.
- Specialized emission cannot proceed on failed inference.
