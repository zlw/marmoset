# E62: Result[a, b] with complex type args
enum Result[a, b] = { Ok(a), Err(b) }
let x: Result[Str, Int] = Result.Ok("hello")
let v = match x {
  case Result.Ok(s): s
  case Result.Err(_): "error"
}
puts(v)  # output: hello
