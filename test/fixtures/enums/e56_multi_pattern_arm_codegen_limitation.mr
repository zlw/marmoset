# E56: Multi-pattern arm hits codegen limitation
enum Color = { Red, Orange, Yellow, Green, Blue, Purple }
let c = Color.Orange
let temp = match c {
  case Color.Red | Color.Orange | Color.Yellow: "warm"  # error: Multiple patterns
  case Color.Green | Color.Blue | Color.Purple: "cool"
}
puts(temp)
