# E56: Multi-pattern enum arm lowers cleanly
enum Color = { Red, Orange, Yellow, Green, Blue, Purple }
let c = Color.Orange
let temp = match c {
  case Color.Red | Color.Orange | Color.Yellow: "warm"
  case Color.Green | Color.Blue | Color.Purple: "cool"
}
puts(temp)  # output: warm
