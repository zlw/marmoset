# E78: Enum variant wrapping a record
enum Option[a] = { Some(a), None }
let x = Option.Some({ name: "alice", age: 30 })
let result = match x {
  case Option.Some(r): r.name
  case Option.None: "nobody"
}
puts(result)  # output: alice
