# E25: Nested enum - Outer Empty case
enum Inner = { A(Int), B }
enum Outer = { Wrap(Inner), Empty }
let x = Outer.Empty
let result = match x {
  case Outer.Wrap(i): match i {
    case Inner.A(n): n
    case Inner.B: 0
  }
  case Outer.Empty: 0 - 1
}
puts(result)  # output: -1
