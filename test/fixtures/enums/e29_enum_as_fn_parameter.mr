# E29: Enum as function parameter
enum direction { north south east west }
let is_north = fn(d: direction) -> int {
  match d {
    direction.north: 1
    _: 0
  }
}
puts(is_north(direction.north))  # output: 1
