# E46: Enum variant with if-expression as payload
enum option[a] { some(a) none }
let flag = true
let x = option.some(if (flag) { 100 } else { 0 })
let result = match x {
  option.some(v): v
  option.none: 0
}
puts(result)  # output: 100
