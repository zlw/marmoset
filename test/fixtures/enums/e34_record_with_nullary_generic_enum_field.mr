# E34: Record with nullary generic enum field infers from match context
enum Option[a] = { Some(a), None }
let r = { value: Option.None }
let out = match r.value {
  case Option.Some(v): v
  case Option.None: 0
}
puts(out)  # output: 0
