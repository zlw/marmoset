# E11: Function parameter with enum type Option[Int]
enum Option[a] = { Some(a), None }
fn unwrap(x: Option[Int]) -> Int = {
  match x {
    case Option.Some(v): v
    case Option.None: 0
  }
}
unwrap(Option.Some(42))
