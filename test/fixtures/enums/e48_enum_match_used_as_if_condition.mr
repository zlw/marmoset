# E48: Enum match used as if condition
enum Option[a] = { Some(a), None }
let x = Option.Some(42)
let is_some = match x {
  case Option.Some(_): true
  case Option.None: false
}
let out = if (is_some) { "yes" } else { "no" }
puts(out)  # output: yes
