# E43: Enum variant with precomputed arithmetic payload
enum option[a] { some(a) none }
let val = 3 * 5
let x = option.some(val)
let result = match x {
  option.some(v): v
  option.none: 0
}
puts(result)  # output: 15
