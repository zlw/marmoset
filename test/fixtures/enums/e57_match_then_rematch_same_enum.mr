# E57: Match then re-match same enum
enum option[a] { some(a) none }
let x = option.some(99)
let y = match x {
  option.some(v): option.some(v)
  option.none: option.none()
}
let result = match y {
  option.some(v): v
  option.none: 0
}
puts(result)  # output: 99
