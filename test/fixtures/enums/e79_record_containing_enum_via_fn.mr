# E79: Record containing enum accessed through function
enum Option[a] = { Some(a), None }
let r = { value: Option.Some(5), label: "test" }
fn extract(opt: Option[Int]) -> Int = {
  match opt { case Option.Some(v): v case Option.None: 0 }
}
puts(extract(r.value))  # output: 5
