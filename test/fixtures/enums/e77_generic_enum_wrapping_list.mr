# E77: Generic enum wrapping a List
enum Option[a] = { Some(a), None }
let x = Option.Some([10, 20, 30])
let result = match x {
  case Option.Some(lst): len(lst)
  case Option.None: 0
}
puts(result)  # output: 3
