# E33: Record with enum payload field - Some
enum Option[a] = { Some(a), None }
let r = { value: Option.Some(42) }
let out = match r.value {
  case Option.Some(v): v
  case Option.None: 0
}
puts(out)  # output: 42
