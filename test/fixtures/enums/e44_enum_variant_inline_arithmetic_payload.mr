# E44: Enum variant with inline arithmetic as payload
enum Option[a] = { Some(a), None }
let x = Option.Some(3 * 5)
let result = match x {
  case Option.Some(v): v
  case Option.None: 0
}
puts(result)  # output: 15
