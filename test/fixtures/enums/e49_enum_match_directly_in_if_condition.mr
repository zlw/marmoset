# E49: Enum match directly in if condition
enum Option[a] = { Some(a), None }
let x = Option.Some(1)
let out = if (match x { case Option.Some(_): true case Option.None: false }) {
  "present"
} else {
  "absent"
}
puts(out)  # output: present
