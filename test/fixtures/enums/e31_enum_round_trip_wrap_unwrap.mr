# E31: Enum round-trip through wrap/unwrap
enum Option[a] = { Some(a), None }
fn wrap(x: Int) -> Option[Int] = Option.Some(x)
fn unwrap(x: Option[Int]) -> Int = {
  match x { case Option.Some(v): v case Option.None: 0 }
}
puts(unwrap(wrap(42)))  # output: 42
