# E75: Function with enum match in body
enum Option[a] = { Some(a), None }
fn describe(x: Option[Int]) -> Str = {
  match x {
    case Option.Some(_): "matched"
    case Option.None: "empty"
  }
}
puts(describe(Option.Some(42)))  # output: matched
