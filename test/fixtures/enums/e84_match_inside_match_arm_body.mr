# E84: Match inside match arm body
enum color { red green blue }
enum size { small large }
let c = color.green
let s = size.small
let out = match c {
  color.red: "red"
  color.green: match s {
    size.small: "deep"
    size.large: "wide"
  }
  color.blue: "blue"
}
puts(out)  # output: deep
