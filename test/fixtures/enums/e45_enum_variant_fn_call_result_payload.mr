# E45: Enum variant with function call Result as payload
enum Option[a] = { Some(a), None }
fn double(n: Int) -> Int = n * 2
let x = Option.Some(double(5))
let result = match x {
  case Option.Some(v): v
  case Option.None: 0
}
puts(result)  # output: 10
