# E83: Match arm with effectful body (puts)
enum Option[a] = { Some(a), None }
let x = Option.Some("hello")
match x {  # output: hello
  case Option.Some(v): puts(v)
  case Option.None: puts("nothing")
}
