# E81: Toolchain stdlib exposes Result with canonical constructors
let x: Result[Int, Str] = Result.Success(100)
match x {
  case Result.Success(value): puts(value)  # output: 100
  case Result.Failure(_): puts(0)
}
