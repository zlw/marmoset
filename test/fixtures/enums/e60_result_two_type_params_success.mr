# E60: Result[a, b] with two type params - success case
enum Result[a, b] = { Ok(a), Err(b) }
let x: Result[Int, Str] = Result.Ok(42)
let v = match x {
  case Result.Ok(n): n
  case Result.Err(_): 0
}
puts(v)  # output: 42
