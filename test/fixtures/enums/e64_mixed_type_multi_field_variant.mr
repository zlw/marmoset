# E64: Mixed-type multi-field variant
enum Tagged = { Item(Int, Str, Bool), Nothing }
let x = Tagged.Item(42, "hello", true)
let out = match x {
  case Tagged.Item(_, s, _): s
  case Tagged.Nothing: "Nothing"
}
puts(out)  # output: hello
