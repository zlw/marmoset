# E14: Multi-field variant extraction
enum HttpResponse = { Ok(Int, Str), Error(Int, Str) }
let x = HttpResponse.Ok(200, "OK")
match x {
  case HttpResponse.Ok(code, _): code
  case HttpResponse.Error(code, _): code
}
