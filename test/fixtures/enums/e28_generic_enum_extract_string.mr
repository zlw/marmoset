# E28: Generic enum - extract Str value
enum Box[a] = { Full(a), Empty }
fn unwrap_str(x: Box[Str]) -> Str = {
  match x { case Box.Full(v): v case Box.Empty: "" }
}
puts(unwrap_str(Box.Full("hello")))  # output: hello
