# E13: Nested enum types Option[List[Int]]
enum Option[a] = { Some(a), None }
fn f(x: Option[List[Int]]) -> Int = {
  match x {
    case Option.Some(lst): len(lst)
    case Option.None: 0
  }
}
f(Option.Some([1, 2, 3]))
