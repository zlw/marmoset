# R022: Derive show for record
# output: { x: 1, y: 2 }
trait show[a] {
  fn show(x: a) -> string
}
type point = { x: int, y: int }
derive show for point;
let p: point = { x: 1, y: 2 }
puts(p.show())
