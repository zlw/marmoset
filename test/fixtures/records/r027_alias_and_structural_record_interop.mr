# R027: Alias and structural record params interoperate through wrapper
alias Point = { x: Int, y: Int }
fn add(a: Point, b: { x: Int, y: Int }) -> Int = a.x + b.y
let p: Point = { x: 1, y: 2 }
let q = { y: 4, x: 3 }
puts(add(p, q))  # output: 5
