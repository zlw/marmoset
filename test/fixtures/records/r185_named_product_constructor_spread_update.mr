# R185: Named product constructor spread preserves nominal result
type Point = { x: Int, y: Int }
let p = Point(x: 1, y: 2)
let q = Point(...p, x: 10)
puts(q.x + q.y)  # output: 12
puts(p.x + p.y)  # output: 3
