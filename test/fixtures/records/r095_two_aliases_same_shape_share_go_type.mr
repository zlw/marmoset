# R095: Two aliases same shape share Go type
alias Point = { x: Int, y: Int }
alias Vec2d = { x: Int, y: Int }
let p: Point = { x: 1, y: 2 }
let v: Vec2d = { x: 3, y: 4 }
fn sum_xy(r) = { r.x + r.y }
puts(sum_xy(p) + sum_xy(v) - sum_xy({ x: 1, y: 0 }))  # output: 9
