# R025: Reordered record literal still resolves derived show
# output: { x: 1, y: 2 }
trait show[a] {
  fn show(x: a) -> string
}
type point = { x: int, y: int }
derive show for point;
let p: point = { y: 2, x: 1 }
puts(p.show())
