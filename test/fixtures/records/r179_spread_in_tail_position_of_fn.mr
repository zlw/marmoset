# R179: Spread in tail position of fn body
# output: 12
type point = { x: int, y: int }
let set_x = fn(p: point, v: int) -> point { { ...p, x: v } }
let p: point = { x: 1, y: 2 }
let q = set_x(p, 10)
puts(q.x + q.y)
