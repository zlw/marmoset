# R015: Typed local wrapper preserves wrapped record fields
# output: 7
let outer = fn(r: { x: int }) {
  let mk = fn(v: { x: int }) { { inner: v } }
  mk(r)
}
let o = outer({ x: 7 })
puts(o.inner.x)
