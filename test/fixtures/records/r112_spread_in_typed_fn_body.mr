# R112: Spread in typed fn body (non-polymorphic)
type point = { x: int, y: int }
let update_x = fn(p: point, new_x: int) -> point { { ...p, x: new_x } }
let p: point = { x: 1, y: 2 }
let q = update_x(p, 10)
puts(q.x + q.y)  # output: 12
