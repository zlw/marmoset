# TH04: Multiple inherent methods on same type
# output: 15
type point = { x: int, y: int }
impl point {
  fn get_x(p: point) -> int { p.x }
  fn get_y(p: point) -> int { p.y }
  fn sum(p: point) -> int { p.x + p.y }
}
let p: point = { x: 7, y: 8 }
puts(p.sum())
