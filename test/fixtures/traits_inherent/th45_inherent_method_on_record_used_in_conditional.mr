# TH45: Inherent method on record used in conditional
type Point = { x: Int, y: Int }
impl Point = {
  fn sum(p: Point) -> Int = p.x + p.y
}
let p: Point = { x: 5, y: 3 }
if (p.sum() > 0) {  # output: positive
  puts("positive")
} else {
  puts("negative")
}
