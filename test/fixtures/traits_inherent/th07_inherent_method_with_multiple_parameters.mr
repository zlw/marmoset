# TH07: Inherent method with multiple parameters
impl Int = {
  fn clamp(x: Int, lo: Int, hi: Int) -> Int = {
    if (x < lo) { lo } else { if (x > hi) { hi } else { x } }
  }
}
let n = 7
puts(n.clamp(0, 10))  # output: 7
