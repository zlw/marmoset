# TH49: Inherent impl target can be a transparent type definition
type Total = Int

impl Total = {
  fn plus_one(total: Total) -> Int = total + 1
}

let total: Total = 41
puts(total.plus_one())  # output: 42
