# TH03: Inherent method on record type alias
type Point = { x: Int, y: Int }
impl Point = {
  fn sum(p: Point) -> Int = {
    p.x + p.y
  }
}
let p: Point = { x: 10, y: 20 }
puts(p.sum())  # output: 30
