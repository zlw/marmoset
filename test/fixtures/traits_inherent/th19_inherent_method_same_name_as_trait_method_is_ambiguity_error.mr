# TH19: Inherent method same name as trait method coexists (inherent wins on dot call)
trait Describable[a] = {
  fn describe(x: a) -> Str
}
impl Describable[Int] = {
  fn describe(x: Int) -> Str = "trait"
}
impl Int = {
  fn describe(x: Int) -> Str = "inherent"
}
puts(1)  # output: 1
