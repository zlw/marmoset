# TH26: Inherent method on type that also has field access
# output: 35
type point = { x: int, y: int }
impl point {
  fn sum(p: point) -> int { p.x + p.y }
}
let p: point = { x: 15, y: 20 }
puts(p.sum())
