# TH06: Inherent method accessing receiver fields
type rect = { w: int, h: int }
impl rect {
  fn area(r: rect) -> int {
    r.w * r.h
  }
}
let r: rect = { w: 10, h: 11 }
puts(r.area())  # output: 110
