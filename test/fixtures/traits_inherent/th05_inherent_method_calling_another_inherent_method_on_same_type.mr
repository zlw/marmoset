# TH05: Inherent method calling another inherent method on same type
impl Int = {
  fn twice(x: Int) -> Int = x + x
  fn quadruple(x: Int) -> Int = Int.twice(Int.twice(x))
}
let n = 5
puts(Int.quadruple(n))  # output: 20
