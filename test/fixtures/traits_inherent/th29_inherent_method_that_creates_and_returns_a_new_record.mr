# TH29: Inherent method that creates and returns a new record
type Box = { value: Int }
impl Int = {
  fn boxed(x: Int) -> Box = {value: x}
}
let n = 99
let b = Int.boxed(n)
puts(b.value)  # output: 99
