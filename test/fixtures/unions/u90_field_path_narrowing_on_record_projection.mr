# U90: Field-path narrowing works on direct record projections
type Box = { value: Int | Str }

fn render(box: Box) -> Str = {
  if (box.value is Int) {
    box.value.show()
  } else {
    box.value
  }
}

puts(render(Box(value: 1)))  # output: 1
