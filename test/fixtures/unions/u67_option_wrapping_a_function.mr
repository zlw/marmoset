# U67: Option wrapping a function
enum Option[a] = { Some(a), None }
fn f(x: Int) -> Int = x * 2
let wrapped = Option.Some(f)
let out = match wrapped {
  case Option.Some(func): func(5)
  case Option.None: 0
}
puts(out)  # output: 10
