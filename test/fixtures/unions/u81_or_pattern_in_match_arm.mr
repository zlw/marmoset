# U81: Or-pattern in match arm (codegen limitation)
type Color = { Red, Green, Blue, Yellow }  # error: *
let c = Color.Red
let kind = match c {
  case Color.Red | Color.Blue: "primary"
  case _: "other"
}
puts(kind)
