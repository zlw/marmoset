# U89: Pattern uses wrong enum name for scrutinee
enum Option[a] = { Some(a), None }  # error: *
enum Result[a, e] = { Ok(a), Err(e) }
let x = Option.Some(1)
match x {
  case Result.Ok(v): v
  case Result.Err(_): 0
}
