# U68: Enum built in if/else, then matched
enum Option[a] = { Some(a), None }
let x = 5
let opt: Option[Str] = if (x > 0) { Option.Some("positive") } else { Option.None() }
let out = match opt {
  case Option.Some(s): s
  case Option.None: "zero or neg"
}
puts(out)  # output: positive
