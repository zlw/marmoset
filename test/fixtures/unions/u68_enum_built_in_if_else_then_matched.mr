# U68: Enum built in if/else, then matched
enum option[a] { some(a) none }
let x = 5
let opt: option[string] = if (x > 0) { option.some("positive") } else { option.none() }
let out = match opt {
  option.some(s): s
  option.none: "zero or neg"
}
puts(out)  # output: positive
