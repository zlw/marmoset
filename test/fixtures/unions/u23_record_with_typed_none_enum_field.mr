# U23: Record with typed None enum field accessed after let binding
enum Option[a] = { Some(a), None }
let n: Option[Str] = Option.None()
let r = { name: "hello", data: n }
let d = r.data
let out = match d {
  case Option.Some(v): v
  case Option.None: r.name
}
puts(out)  # output: hello
