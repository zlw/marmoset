# U71: Two enums with same variant name (used separately)
enum Result1[a] = { Ok(a), Fail }
enum Result2[a] = { Ok(a), Fail }
let r1 = Result1.Ok(1)
let a = match r1 { case Result1.Ok(v): v case Result1.Fail: 0 }
puts(a)  # output: 1
