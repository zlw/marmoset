# U70: Variable name same as variant name
type Option[a] = { Some(a), None }
let some = 42
let x = Option.Some(some)
let out = match x {
  case Option.Some(v): v
  case Option.None: 0
}
puts(out)  # output: 42
