# U50: Enum constructor as direct function argument
enum Option[a] = { Some(a), None }
fn unwrap(x: Option[Int]) -> Int = {
  match x { case Option.Some(v): v case Option.None: 0 }
}
puts(unwrap(Option.Some(42)))  # output: 42
