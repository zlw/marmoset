# U73: Let binding inside match arm body
type Option[a] = { Some(a), None }
let x = Option.Some(42)
let out = match x {
  case Option.Some(v): {
    let incremented = v + 1
    incremented
  }
  case Option.None: 0
}
puts(out)  # output: 43
