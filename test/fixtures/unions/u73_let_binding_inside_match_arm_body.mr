# U73: Let binding inside match arm body (parser limitation)
# error: *
enum option[a] { some(a) none }
let x = option.some(42)
let out = match x {
  option.some(v): {
    let incremented = v + 1
    incremented
  }
  option.none: 0
}
puts(out)
