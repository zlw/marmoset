# U40: result with record ok value
# output: alice
enum result[a, e] { ok(a) err(e) }
let r: result[{name: string}, int] = result.ok({name: "alice"})
let out = match r {
  result.ok(v): v.name
  result.err(_): "error"
}
puts(out)
