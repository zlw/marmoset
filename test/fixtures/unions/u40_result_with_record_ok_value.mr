# U40: Result with record Ok value
enum Result[a, e] = { Ok(a), Err(e) }
let r: Result[{name: Str}, Int] = Result.Ok({name: "alice"})
let out = match r {
  case Result.Ok(v): v.name
  case Result.Err(_): "error"
}
puts(out)  # output: alice
