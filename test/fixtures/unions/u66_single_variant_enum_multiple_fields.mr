# U66: Single-variant enum with multiple fields
enum Entry = { Item(Str, Int) }
let e = Entry.Item("hello", 42)
let out = match e {
  case Entry.Item(s, n): s + " " + Show.show(n)
}
puts(out)  # output: hello 42
