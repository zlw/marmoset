# U44: String concat with show in match arm body
type Option[a] = { Some(a), None }
let x = Option.Some(42)
let out = match x {
  case Option.Some(v): "value: " + v.show()
  case Option.None: "nothing"
}
puts(out)  # output: value: 42
