# U08: Nested type narrowing
fn outer(x: Int | Str) -> Int = {
   if (x is Int) {
       let inner = (y: Int | Bool) -> if (y is Int) { y } else { 0 };
       inner(x)
   } else {
       0
   }
 }
outer(42)
