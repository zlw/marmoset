# U27: Match in tail position of function body
type Option[a] = { Some(a), None }
fn unwrap(x: Option[Int]) -> Int = {
  match x {
    case Option.Some(v): v
    case Option.None: 0
  }
}
puts(unwrap(Option.Some(10)))  # output: 10
