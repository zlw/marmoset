# U42: Function call in match arm body
enum Option[a] = { Some(a), None }
let x = Option.Some([1, 2, 3, 4, 5])
let out = match x {
  case Option.Some(arr): len(arr)
  case Option.None: 0
}
puts(out)  # output: 5
