# U07: Three-way union exhaustiveness
fn f(x: Int | Str | Bool) -> Str = {
   if (x is Int) {
       "Int"
   } else {
       if (x is Str) {
           x
       } else {
           "Bool"
       }
   }
 }
f(true)
