# U61: Wildcard for individual fields in multi-field variant
type Http = { Response(Int, Str) }
let r = Http.Response(200, "OK")
let out = match r {
  case Http.Response(code, _): code
}
puts(out)  # output: 200
