# U39: option wrapping record type
enum option[a] { some(a) none }
let p = { x: 1, y: 2 }
let wrapped = option.some(p)
let out = match wrapped {
  option.some(r): r.x + r.y
  option.none: 0
}
puts(out)  # output: 3
