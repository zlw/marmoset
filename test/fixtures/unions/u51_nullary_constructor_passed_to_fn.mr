# U51: Nullary constructor passed to function
enum option[a] { some(a) none }
let n: option[int] = option.none()
let unwrap = fn(x: option[int]) -> int {
  match x { option.some(v): v option.none: 0 }
}
puts(unwrap(n))  # output: 0
