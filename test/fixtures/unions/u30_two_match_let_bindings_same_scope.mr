# U30: Two match-let bindings in same scope
# output: 50
enum option[a] { some(a) none }
let x = option.some(20)
let y = option.some(30)
let a = match x { option.some(v): v option.none: 0 }
let b = match y { option.some(v): v option.none: 0 }
puts(a + b)
