# U31: Wildcard after specific variant patterns
enum Color = { Red, Green, Blue, Yellow }
let c = Color.Yellow
let name = match c {
  case Color.Red: "Red"
  case Color.Green: "Green"
  case _: "default"
}
puts(name)  # output: default
