# U31: Wildcard after specific variant patterns
# output: default
enum color { red green blue yellow }
let c = color.yellow
let name = match c {
  color.red: "red"
  color.green: "green"
  _: "default"
}
puts(name)
