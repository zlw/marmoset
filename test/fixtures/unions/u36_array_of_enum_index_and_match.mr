# U36: Array of enum, index and match
enum Option[a] = { Some(a), None }
let arr = [Option.Some(10), Option.Some(20), Option.None()]
let first = arr[0]
let out = match first {
  case Option.Some(v): v
  case Option.None: 0
}
puts(out)  # output: 10
