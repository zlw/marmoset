# CG03: Pure annotated higher-order caller rejects unknown callback calls conservatively
fn hof(f) -> Int = f(1) # error: Pure function (declared with ->) cannot call effectful operations
fn eff(x: Int) => Int = x
puts(hof(eff))
