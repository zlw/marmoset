# CG02: Unannotated higher-order caller handles pure and effectful callbacks
fn hof(f) = f(1)
fn pure(x: Int) -> Int = x + 1
fn eff(x: Int) => Int = {
  puts(x)
  x + 2
}
# output: 2
# output: 1
puts(hof(pure))
puts(hof(eff))  # output: 3
