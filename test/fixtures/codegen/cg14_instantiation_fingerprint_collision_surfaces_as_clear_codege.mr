# CG14: Instantiation fingerprint collision surfaces as clear codegen error
enum B = { V } # error: Codegen error: instantiation fingerprint collision for 'id'
enum A[t] = { Wrap(t) }
enum A_B = { Wrap }
fn id(x) = x
let x = id(A_B.Wrap)
let y = id(A.Wrap(B.V))
puts(1)
