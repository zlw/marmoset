# CG09: Pure annotated recursive function with effectful body is rejected
let loop = fn(n: int) -> int {  # error: Pure function (declared with ->) cannot call effectful operations
  if (n == 0) {
    0
  } else {
    puts(n)
    loop(n - 1)
  }
}
puts(loop(2))
