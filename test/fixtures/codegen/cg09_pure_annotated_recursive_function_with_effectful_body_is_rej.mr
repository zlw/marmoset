# CG09: Pure annotated recursive function with effectful body is rejected
fn loop(n: Int) -> Int = { # error: Pure function (declared with ->) cannot call effectful operations
  if (n == 0) {
    0
  } else {
    puts(n)
    loop(n - 1)
  }
}
puts(loop(2))
