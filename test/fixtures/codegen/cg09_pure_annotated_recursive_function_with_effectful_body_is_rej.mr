# CG09: Pure annotated recursive function with effectful body is rejected
# error: Pure function (declared with ->) cannot call effectful operations
let loop = fn(n: int) -> int {
  if (n == 0) {
    0
  } else {
    puts(n)
    loop(n - 1)
  }
}
puts(loop(2))
