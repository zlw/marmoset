# CG38: Recursive function with match in tail position
# output: 55
let fib_helper = fn(n: int, a: int, b: int) -> int {
  match n {
    0: a
    _: fib_helper(n - 1, b, a + b)
  }
}
puts(fib_helper(10, 0, 1))
