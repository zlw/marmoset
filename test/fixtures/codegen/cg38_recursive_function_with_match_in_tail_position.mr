# CG38: Recursive function with match in tail position
fn fib_helper(n: Int, a: Int, b: Int) -> Int = {
  match n {
    case 0: a
    case _: fib_helper(n - 1, b, a + b)
  }
}
puts(fib_helper(10, 0, 1))  # output: 55
