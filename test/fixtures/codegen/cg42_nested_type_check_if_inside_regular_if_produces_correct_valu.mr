# CG42: Nested type-check if inside regular if produces correct value
let f = fn(flag: bool, x: int | string) -> int {
  if (flag) {
    if (x is int) { x + 9 } else { 0 }
  } else {
    0
  }
}
puts(f(true, 1))  # output: 10
