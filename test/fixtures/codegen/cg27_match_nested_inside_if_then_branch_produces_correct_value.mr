# CG27: Match nested inside if-then branch produces correct value
fn f(flag: Bool, x: Int) -> Int = {
  if (flag) {
    match x {
      case 1: 10
      case 2: 20
      case _: 30
    }
  } else {
    0
  }
}
puts(f(true, 2))  # output: 20
