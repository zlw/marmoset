# CG08: Unannotated recursive function with effectful body infers and runs as effectful
fn loop(n: Int) = {
  if (n == 0) {
    0
  } else {
    puts(n)
    loop(n - 1)
  }
}
# output: 2
# output: 1
# output: 0
puts(loop(2))
