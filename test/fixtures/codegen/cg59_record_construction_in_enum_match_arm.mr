# CG59: Record construction in enum match arm
enum Option[a] = { Some(a), None }
let val = Option.Some(42)
let p = match val {
  case Option.Some(v): { x: v, y: 0 }
  case Option.None: { x: 0, y: 0 }
}
puts(p.x)  # output: 42
