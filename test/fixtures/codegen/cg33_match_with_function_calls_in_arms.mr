# CG33: Match with function calls in arms
# output: 25
let double = fn(x: int) -> int { x * 2 }
let triple = fn(x: int) -> int { x * 3 }
let n = 2
let result = match n {
  1: double(10)
  2: double(10) + 5
  _: triple(10)
}
puts(result)
