# CG39: Multiple if-expressions as sequential statements in function body
# output: 3
let f = fn(a: bool, b: bool) => int {
  let x = if (a) { 1 } else { 0 }
  let y = if (b) { 2 } else { 0 }
  x + y
}
puts(f(true, true))
