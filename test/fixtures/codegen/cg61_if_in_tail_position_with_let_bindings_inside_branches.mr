# CG61: if in tail position with let bindings inside branches
fn f(flag: Bool) -> Int = {
  if (flag) {
    let a = 10
    let b = 20
    a + b
  } else {
    let c = 5
    c
  }
}
puts(f(true))  # output: 30
