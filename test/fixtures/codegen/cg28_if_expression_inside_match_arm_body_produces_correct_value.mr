# CG28: if-expression inside match arm body produces correct value
fn f(x: Int, flag: Bool) -> Int = {
  match x {
    case 1: if (flag) { 99 } else { 0 }
    case _: 50
  }
}
puts(f(1, true))  # output: 99
