# CG36: Outer if in let binding with inner match in tail
let flag = true
let n = 3
let x = if (flag) {
  match n {
    case 1: 10
    case 2: 20
    case 3: 30
    case _: 0
  }
} else {
  0
}
puts(x)  # output: 30
