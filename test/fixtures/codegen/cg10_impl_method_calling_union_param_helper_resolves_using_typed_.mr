# CG10: Impl method calling union-param helper resolves using typed env
fn helper(x: Int | Str) -> Str = {
  if (x is Int) { "int" } else { "string" }
}
trait Show[a] = { fn show(x: a) -> Str }
impl Show[Int] = {
  fn show(x: Int) -> Str = helper(x)
}
puts(Show.show(1))  # output: int
