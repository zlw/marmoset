# CG11: Specialized body codegen failure is surfaced cleanly
fn f(x: Int) -> Int = { # error: Codegen error: Multiple patterns per arm not yet supported in codegen
  match x {
    case 1 | 2: 1
    case _: 0
  }
}
puts(f(1))
