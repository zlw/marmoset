# CG62: if in tail position with let bindings takes else branch
fn f(flag: Bool) -> Int = {
  if (flag) {
    let a = 10
    let b = 20
    a + b
  } else {
    let c = 5
    c
  }
}
puts(f(false))  # output: 5
