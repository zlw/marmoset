# CG71: Enum match in tail position if takes else branch
enum Option[a] = { Some(a), None }
fn check(o: Option[Int]) -> Int = {
  match o {
    case Option.Some(v): if (v > 10) { 100 } else { v }
    case Option.None: 0
  }
}
puts(check(Option.Some(7)))  # output: 7
