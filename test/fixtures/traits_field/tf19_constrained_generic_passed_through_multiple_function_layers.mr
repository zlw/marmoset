# TF19: Constrained generic shape passed through multiple function layers
shape HasName = {
  name: Str
}
fn inner[t: HasName](v: t) -> Str = "inner:" + v.name
fn outer[t: HasName](v: t) -> Str = inner(v)
let r = { name: "bob", extra: 1 }
puts(outer(r))  # output: inner:bob
