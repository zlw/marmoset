# TF16: Constrained generic field used in if-expression
trait HasVal = {
  val: Int
}
fn classify[t: HasVal](v: t) -> Str = {
  if (v.val > 10) {
    "big"
  } else {
    "small"
  }
}
let r = { val: 42 }
puts(classify(r))  # output: big
