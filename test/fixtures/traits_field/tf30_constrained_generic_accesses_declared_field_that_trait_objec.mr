# TF30: Constrained generic accesses declared field that trait object would also expose
trait HasName = {
  name: Str
}
fn get_name[t: HasName](v: t) -> Str = v.name
let r = { name: "test", extra: 1 }
puts(get_name(r))  # output: test
