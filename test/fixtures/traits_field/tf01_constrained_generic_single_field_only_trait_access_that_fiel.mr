# TF01: Constrained generic: single field-only trait, access that field
trait has_x {
  x: int
}
let get_x = fn[t: has_x](v: t) -> int {
  v.x
};
let r = { x: 42, y: 99 }
puts(get_x(r))  # output: 42
