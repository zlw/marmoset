# TF04: Constrained generic: access field absent from all constraints
trait HasName = {
  name: Str
}
fn get_missing[t: HasName](v: t) -> Int = v.missing_field  # error: not guaranteed by constraints
get_missing({ name: "test", missing_field: 1 })
