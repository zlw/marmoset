# TF07: Constrained generic: record missing required field
trait has_name {  # error: missing-field
  name: string
}
let get_name = fn[t: has_name](v: t) -> string {
  v.name
};
get_name({ age: 42 })
