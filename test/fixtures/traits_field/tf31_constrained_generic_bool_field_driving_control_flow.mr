# TF31: Constrained generic: Bool shape field driving control flow
shape HasActive = {
  active: Bool
}
fn status[t: HasActive](v: t) -> Str = if (v.active) { "active" } else { "inactive" }
let r = { active: true, name: "test" }
puts(status(r))  # output: active
