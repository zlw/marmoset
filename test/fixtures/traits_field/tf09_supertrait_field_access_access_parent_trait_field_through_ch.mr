# TF09: Wider shape constraint exposes the shared field
shape HasName = {
  name: Str
}
shape Person = {
  name: Str
  age: Int
}
fn get_name[t: Person](v: t) -> Str = v.name
let p = { name: "hello", age: 30 }
puts(get_name(p))  # output: hello
