# TF35: Supertrait chain: call site record missing grandparent field
trait HasA = {
  a: Int
}
trait HasB: HasA = {
  b: Int
}
fn get_a[t: HasB](v: t) -> Int = v.a
get_a({ b: 1 })  # error: missing-field
