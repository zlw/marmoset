# TF35: Wider shape still rejects a record missing a required field
shape HasA = {
  a: Int
}
shape HasB = {
  a: Int
  b: Int
}
fn get_a[t: HasB](v: t) -> Int = v.a
get_a({ b: 1 })  # error: missing-field
