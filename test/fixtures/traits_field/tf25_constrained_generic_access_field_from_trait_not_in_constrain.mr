# TF25: Constrained generic: access field from shape not in constraints
shape HasName = {
  name: Str
}
shape HasAge = {
  age: Int
}
fn get_age[t: HasName](v: t) -> Int = v.age  # error: not guaranteed by constraints
get_age({ name: "alice", age: 42 })
