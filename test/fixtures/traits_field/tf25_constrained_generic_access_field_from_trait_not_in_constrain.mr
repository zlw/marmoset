# TF25: Constrained generic: access field from trait not in constraints
trait HasName = {
  name: Str
}
trait HasAge = {
  age: Int
}
fn get_age[t: HasName](v: t) -> Int = v.age  # error: not guaranteed by constraints
get_age({ name: "alice", age: 42 })
