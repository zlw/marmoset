# TF25: Constrained generic: access field from trait not in constraints
trait has_name {
  name: string
}
trait has_age {
  age: int
}
let get_age = fn[t: has_name](v: t) -> int {
  v.age  # error: not guaranteed by constraints
};
get_age({ name: "alice", age: 42 })
