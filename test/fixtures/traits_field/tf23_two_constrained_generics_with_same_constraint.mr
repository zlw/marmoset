# TF23: Two constrained generics with same constraint
trait HasName = {
  name: Str
}
fn concat_names[a: HasName, b: HasName](x: a, y: b) -> Str = x.name + " " + y.name
let r1 = { name: "hello", id: 1 }
let r2 = { name: "world", active: true }
puts(concat_names(r1, r2))  # output: hello world
