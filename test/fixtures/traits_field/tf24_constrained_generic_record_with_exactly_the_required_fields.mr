# TF24: Constrained generic: record with exactly the required shape fields
shape HasTag = {
  tag: Str
}
fn get_tag[t: HasTag](v: t) -> Str = v.tag
let r = { tag: "exact" }
puts(get_tag(r))  # output: exact
