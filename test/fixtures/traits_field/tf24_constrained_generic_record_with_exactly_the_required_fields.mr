# TF24: Constrained generic: record with exactly the required fields
# output: exact
trait has_tag {
  tag: string
}
let get_tag = fn[t: has_tag](v: t) -> string {
  v.tag
};
let r = { tag: "exact" }
puts(get_tag(r))
