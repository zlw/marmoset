# TF17: Constrained generic field used in match expression
shape HasFlag = {
  flag: Bool
}
fn check[t: HasFlag](v: t) -> Str = match v.flag {
  case true: "yes"
  case false: "no"
}
let r = { flag: true }
puts(check(r))  # output: yes
