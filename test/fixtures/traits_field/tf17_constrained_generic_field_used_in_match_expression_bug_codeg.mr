# TF17: Constrained generic field used in match expression (BUG: codegen missing return)
# error: missing return
trait has_flag {
  flag: bool
}
let check = fn[t: has_flag](v: t) -> string {
  match v.flag {
    true: "yes"
    false: "no"
  }
};
let r = { flag: true }
puts(check(r))
