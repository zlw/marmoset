# TF17: Constrained generic field used in match expression (BUG: codegen missing return)
trait HasFlag = {  # error: missing return
  flag: Bool
}
fn check[t: HasFlag](v: t) -> Str = match v.flag {
  case true: "yes"
  case false: "no"
}
let r = { flag: true }
puts(check(r))
