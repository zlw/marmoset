# TF05: Constrained generic: access field from first constraint when second is present
trait HasX = {
  x: Int
}
trait HasY = {
  y: Int
}
fn get_x_only[t: HasX & HasY](v: t) -> Int = v.x
let r = { x: 10, y: 20 }
puts(get_x_only(r))  # output: 10
