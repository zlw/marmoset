# TF33: Empty child trait inherits parent fields
trait Base = {
  value: Str
}
trait Child: Base = {}
fn get_value[t: Child](v: t) -> Str = v.value
let r = { value: "inherited" }
puts(get_value(r))  # output: inherited
