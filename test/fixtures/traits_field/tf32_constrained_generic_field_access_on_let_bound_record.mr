# TF32: Constrained generic: field access on let-bound record
# output: stored
trait has_data {
  data: string
}
let extract = fn[t: has_data](v: t) -> string {
  v.data
};
let record = { data: "stored", meta: 123 }
let result = extract(record)
puts(result)
