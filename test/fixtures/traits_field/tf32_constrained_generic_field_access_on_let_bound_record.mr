# TF32: Constrained generic: shape field access on let-bound record
shape HasData = {
  data: Str
}
fn extract[t: HasData](v: t) -> Str = v.data
let record = { data: "stored", meta: 123 }
let result = extract(record)
puts(result)  # output: stored
