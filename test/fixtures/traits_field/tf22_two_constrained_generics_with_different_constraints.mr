# TF22: Two constrained generics with different constraints
trait HasName = {
  name: Str
}
trait HasAge = {
  age: Int
}
fn combine[a: HasName, b: HasAge](x: a, y: b) -> Str = x.name + y.age.show()
let n = { name: "alice" }
let a = { age: 42 }
puts(combine(n, a))  # output: alice42
