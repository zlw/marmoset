# TF13: Multiple constraints with same field name but different types
trait HasXInt = {
  x: Int
}
trait HasXString = {
  x: Str
}
fn get_x[t: HasXInt & HasXString](v: t) -> Int = v.x  # error: Conflicting field type
get_x({ x: 1 })
