# TF13: Multiple shape constraints with same field name but different types
shape HasXInt = {
  x: Int
}
shape HasXString = {
  x: Str
}
fn get_x[t: HasXInt & HasXString](v: t) -> Int = v.x  # error: Conflicting field type
get_x({ x: 1 })
