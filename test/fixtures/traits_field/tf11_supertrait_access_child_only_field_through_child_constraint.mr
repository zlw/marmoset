# TF11: Wider shape constraint exposes its own fields
shape Person = {
  name: Str
  age: Int
}
fn get_age[t: Person](v: t) -> Int = v.age
let p = { name: "hello", age: 99 }
puts(get_age(p))  # output: 99
