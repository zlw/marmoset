# TF11: Supertrait: access child-only field through child constraint
trait HasName = {
  name: Str
}
trait Person: HasName = {
  age: Int
}
fn get_age[t: Person](v: t) -> Int = v.age
let p = { name: "hello", age: 99 }
puts(get_age(p))  # output: 99
