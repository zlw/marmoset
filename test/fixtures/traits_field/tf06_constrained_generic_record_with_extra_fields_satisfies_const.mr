# TF06: Constrained generic: record with extra fields satisfies shape constraint
shape HasName = {
  name: Str
}
fn get_name[t: HasName](v: t) -> Str = v.name
let r = { name: "hi", age: 42, active: true, score: 100 }
puts(get_name(r))  # output: hi
