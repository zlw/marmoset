# TF20: Constrained generic: return shape field value from function
shape HasValue = {
  value: Str
}
fn extract[t: HasValue](v: t) -> Str = v.value
let r = { value: "result" }
puts(extract(r))  # output: result
