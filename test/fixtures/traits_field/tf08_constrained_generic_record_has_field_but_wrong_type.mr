# TF08: Constrained generic: record has field but wrong type
trait has_name {
  name: string
}
let get_name = fn[t: has_name](v: t) -> string {
  v.name
};
get_name({ name: 42 })  # error: does not satisfy trait has_name because field 'name' has type Int but expected String
