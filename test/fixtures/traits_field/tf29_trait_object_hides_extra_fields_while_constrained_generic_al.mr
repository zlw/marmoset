# TF29: Trait object hides extra fields while constrained generic allows access to declared fields
trait HasName = {
  name: Str
}
let x: HasName = { name: "test", extra: 1 }  # error: cannot be used as a type in vNext
puts(x.extra)
