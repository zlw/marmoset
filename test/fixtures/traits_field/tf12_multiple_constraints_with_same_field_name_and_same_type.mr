# TF12: Multiple shape constraints with same field name and same type
shape TagA = {
  label: Str
}
shape TagB = {
  label: Str
}
fn get_label[t: TagA & TagB](v: t) -> Str = v.label
let r = { label: "hi" }
puts(get_label(r))  # output: hi
