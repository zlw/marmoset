# TF27: Constrained TVar preserves constraints: second field access fails
trait HasX = {
  x: Int
}
fn bad[t: HasX](v: t) -> Int = v.x + v.y  # error: not guaranteed by constraints
bad({ x: 1, y: 2 })
