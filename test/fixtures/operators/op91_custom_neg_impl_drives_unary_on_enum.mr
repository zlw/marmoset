# OP91: Custom neg impl drives unary - on enum
enum Wrapper = { Val(Int) }
impl Neg[Wrapper] = {
  fn neg(x: Wrapper) -> Wrapper = x
}
let a = Wrapper.Val(99)
match (-a) {  # output: 99
  case Wrapper.Val(n): puts(n)
}
