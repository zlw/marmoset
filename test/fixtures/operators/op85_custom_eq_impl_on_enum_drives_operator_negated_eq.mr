# OP85: Custom eq impl on enum drives != operator (negated eq)
enum color { red green blue }
impl eq for color {
  fn eq(x: color, y: color) -> bool {
    true
  }
}
puts(color.red != color.blue)  # output: false
