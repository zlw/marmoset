# OP93: Mixed: primitive == and custom enum == in same program
enum Color = { Red, Green, Blue }
impl Eq[Color] = {
  fn eq(x: Color, y: Color) -> Bool = false
}
puts(1 == 1)  # output: true
puts(Color.Red == Color.Green)  # output: false
