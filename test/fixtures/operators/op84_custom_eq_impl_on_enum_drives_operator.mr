# OP84: Custom eq impl on enum drives == operator
enum Color = { Red, Green, Blue }
impl Eq[Color] = {
  fn eq(x: Color, y: Color) -> Bool = {
    true
  }
}
puts(Color.Red == Color.Blue)  # output: true
