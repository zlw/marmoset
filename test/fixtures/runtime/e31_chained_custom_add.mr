enum Boxed = { Val(Int) }
impl Num[Boxed] = {
  fn add(x: Boxed, y: Boxed) -> Boxed = x
  fn sub(x: Boxed, y: Boxed) -> Boxed = x
  fn mul(x: Boxed, y: Boxed) -> Boxed = x
  fn div(x: Boxed, y: Boxed) -> Boxed = x
}
let a = Boxed.Val(10)
let b = Boxed.Val(20)
let c = Boxed.Val(30)
match (a + b + c) {
  case Boxed.Val(n): puts(n)  # output: 10
}
