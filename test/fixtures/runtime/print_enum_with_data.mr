enum Option[a] = { Some(a), None }

impl Show[Option[Int]] = {
  fn show(option: Option[Int]) -> Str = match option {
    case Option.Some(value): "Some(" + Show.show(value) + ")"
    case Option.None: "None"
  }
}

let x = Option.Some(42)
puts(x)  # output: Some(42)
