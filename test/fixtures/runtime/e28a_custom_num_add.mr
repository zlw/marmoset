type Boxed = { Val(Int) }
impl Num[Boxed] = {
  fn add(x: Boxed, y: Boxed) -> Boxed = x
  fn sub(x: Boxed, y: Boxed) -> Boxed = x
  fn mul(x: Boxed, y: Boxed) -> Boxed = x
  fn div(x: Boxed, y: Boxed) -> Boxed = x
}
let a = Boxed.Val(42)
let b = Boxed.Val(99)
match (a + b) {
  case Boxed.Val(n): puts(n)  # output: 42
}
