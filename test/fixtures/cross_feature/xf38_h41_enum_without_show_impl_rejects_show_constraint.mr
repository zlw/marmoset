# XF38: H41: Enum without show impl fails show constraint
enum Color = { Red, Green, Blue }
fn render[a: Show](x: a) -> Str = {
  x.show()
}
puts(render(Color.Red))  # error: does not implement trait
