# XF39: H46: Mixed constraint fails when method trait not implemented
trait Named = {
  name: Str
}
trait Shown[a] = {
  fn show(x: a) -> Str
}
fn get_name[t: Named & Shown](x: t) -> Str = {
  x.name
}
puts(get_name({ name: "alice" }))  # error: does not implement trait Shown
