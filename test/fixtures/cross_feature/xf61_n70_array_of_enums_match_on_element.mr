# XF61: N70: Array of enums, match on element
enum OptionInt = { Some(Int), None }
let arr = [OptionInt.Some(42), OptionInt.None]
match arr[0] {  # output: 42
  case OptionInt.Some(n): puts(n)
  case OptionInt.None: puts(0)
}
