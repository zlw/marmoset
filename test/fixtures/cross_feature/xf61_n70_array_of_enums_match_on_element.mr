# XF61: N70: Array of enums, match on element
enum option_int { some(int) none }
let arr = [option_int.some(42), option_int.none]
match arr[0] {  # output: 42
  option_int.some(n): puts(n)
  option_int.none: puts(0)
}
