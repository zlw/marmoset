# XF69: P79: Inherent method returning enum variant
# output: 42
enum option_int { some(int) none }
impl int {
  fn to_option(x: int) -> option_int {
    option_int.some(x)
  }
}
let result = 42.to_option()
match result {
  option_int.some(n): puts(n)
  option_int.none: puts(0)
}
