# XF69: P79: Inherent method returning enum variant
enum OptionInt = { Some(Int), None }
impl Int = {
  fn to_option(x: Int) -> OptionInt = OptionInt.Some(x)
}
let result = 42.to_option()
match result {  # output: 42
  case OptionInt.Some(n): puts(n)
  case OptionInt.None: puts(0)
}
