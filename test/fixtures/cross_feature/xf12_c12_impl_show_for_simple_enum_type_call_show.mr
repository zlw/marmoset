# XF12: C12: Impl show for simple enum type, call show()
enum Color = { Red, Green, Blue }
impl Show[Color] = {
  fn show(x: Color) -> Str = {
    match x {
      case Color.Red: "red"
      case Color.Green: "green"
      case Color.Blue: "blue"
    }
  }
}
puts(Color.Red.show())  # output: red
