# XF01: A1: Record satisfies two field-only traits at once
trait Named = {
  name: Str
}
trait Aged = {
  age: Int
}
fn describe[t: Named & Aged](x: t) -> Str = {
  x.name + " " + x.age.show()
}
let p = { name: "alice", age: 30, active: true }
puts(describe(p))  # output: alice 30
