# XF01: A1: Record satisfies two structural shapes at once
shape Named = {
  name: Str
}
shape Aged = {
  age: Int
}
fn describe[t: Named & Aged](x: t) -> Str = {
  x.name + " " + Show.show(x.age)
}
let p = { name: "alice", age: 30, active: true }
puts(describe(p))  # output: alice 30
