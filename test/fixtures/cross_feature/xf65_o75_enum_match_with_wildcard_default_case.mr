# XF65: O75: Enum match with wildcard default case
enum Color = { Red, Green, Blue }
fn describe(c: Color) -> Str = {
  match c {
    case Color.Red: "red"
    case _: "other"
  }
}
puts(describe(Color.Blue))  # output: other
