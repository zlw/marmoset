# XF20: D20: Operator result fed into match expression
let x = 5
let y = 3
let diff = x - y
let label = if (diff > 0) { "positive" } else { if (diff == 0) { "zero" } else { "negative" } }
puts(label)  # output: positive
