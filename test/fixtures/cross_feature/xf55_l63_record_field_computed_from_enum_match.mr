# XF55: L63: Record field computed from enum match
enum OptionInt = { Some(Int), None }
let v = OptionInt.Some(42)
let n = match v {
  case OptionInt.Some(x): x
  case OptionInt.None: 0
}
let r = { val: n }
puts(r.val)  # output: 42
