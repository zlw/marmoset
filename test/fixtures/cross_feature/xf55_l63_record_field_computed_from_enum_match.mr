# XF55: L63: Record field computed from enum match
enum option_int { some(int) none }
let v = option_int.some(42)
let n = match v {
  option_int.some(x): x
  option_int.none: 0
}
let r = { val: n }
puts(r.val)  # output: 42
