# XF21: E21: Generic show fn + enum + operator interaction
# output: 42 is big
let classify = fn(n: int) -> string {
  if (n > 10) {
    n.show() + " is big"
  } else {
    n.show() + " is small"
  }
}
puts(classify(42))
