# XF21: E21: Generic show fn + enum + operator interaction
fn classify(n: Int) -> Str = {
  if (n > 10) {
    n.show() + " is big"
  } else {
    n.show() + " is small"
  }
}
puts(classify(42))  # output: 42 is big
