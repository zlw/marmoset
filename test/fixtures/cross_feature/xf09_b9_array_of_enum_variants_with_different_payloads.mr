# XF09: B9: Array of enum variants with different payloads
enum item { num(int) text(string) }
let items = [item.num(42), item.text("hello")]
match items[0] {  # output: 42
  item.num(n): puts(n)
  item.text(s): puts(s)
}
