# XF57: L65: Function return + enum match + arithmetic combined
# output: 42
enum option_int { some(int) none }
let mk = fn() { { val: option_int.some(40) } }
let r = mk()
let n = match r.val {
  option_int.some(x): x + 2
  option_int.none: 0
}
puts(n)
