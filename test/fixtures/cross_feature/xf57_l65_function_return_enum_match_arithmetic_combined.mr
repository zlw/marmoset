# XF57: L65: Function return + enum match + arithmetic combined
enum OptionInt = { Some(Int), None }
fn mk() = { val: OptionInt.Some(40) }
let r = mk()
let n = match r.val {
  case OptionInt.Some(x): x + 2
  case OptionInt.None: 0
}
puts(n)  # output: 42
