# XF46: I54: Show impl + generic constraint + match combined
enum Direction = { Left, Right, Up, Down }
impl Show[Direction] = {
  fn show(d: Direction) -> Str = {
    match d {
      case Direction.Left: "left"
      case Direction.Right: "right"
      case Direction.Up: "up"
      case Direction.Down: "down"
    }
  }
}
fn render[a: Show](x: a) -> Str = x.show()
puts(render(Direction.Left))  # output: left
