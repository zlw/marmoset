# XF02: A2: Spread of trait-typed param into new record
trait Named = {
  name: Str
}
fn wrap[t: Named](x: t) -> { name: Str } = {
  { name: x.name }
}
let result = wrap({ name: "alice", age: 30 })
puts(result.name)  # output: alice
