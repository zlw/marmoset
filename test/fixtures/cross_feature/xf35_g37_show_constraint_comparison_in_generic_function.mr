# XF35: G37: Show constraint + comparison in generic function
fn classify[a: Show & Eq](x: a, threshold: a) -> Str = {
  if (x == threshold) {
    "equal: " + Show.show(x)
  } else {
    "big: " + Show.show(x)
  }
}
puts(classify(100, 42))  # output: big: 100
