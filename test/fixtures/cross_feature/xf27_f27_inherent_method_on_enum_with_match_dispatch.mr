# XF27: F27: Inherent method on enum with match dispatch
enum Box = { Val(Int), Empty }
impl Box = {
  fn unwrap_or(b: Box, fallback: Int) -> Int = {
    match b {
      case Box.Val(n): n
      case Box.Empty: fallback
    }
  }
}
let b = Box.Val(42)
puts(b.unwrap_or(0))  # output: 42
