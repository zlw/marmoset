# XF43: I51: Show of match result from enum
enum OptionInt = { Some(Int), None }
fn unwrap_show(v: OptionInt) -> Str = {
  let n = match v {
    case OptionInt.Some(x): x
    case OptionInt.None: 0
  }
  n.show()
}
puts(unwrap_show(OptionInt.Some(42)))  # output: 42
