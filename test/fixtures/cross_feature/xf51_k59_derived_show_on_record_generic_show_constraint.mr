# XF51: K59: Derived show on record + generic show constraint
type Point = { x: Int, y: Int } derive Show
fn render[a: Show](x: a) -> Str = x.show()
let p = {x: 1, y: 2}
puts(render(p))  # output: { x: 1, y: 2 }
