# XF37: G39: Dual field-only constraints on generic function
trait named {
  name: string
}
trait scored {
  score: int
}
let describe = fn[t: named + scored](x: t) -> string {
  x.name + " " + x.score.show()
}
let p = { name: "alice", score: 42, extra: true }
puts(describe(p))  # output: alice 42
