# XF37: G39: Dual shape constraints on generic function
shape Named = {
  name: Str
}
shape Scored = {
  score: Int
}
fn describe[t: Named & Scored](x: t) -> Str = {
  x.name + " " + x.score.show()
}
let p = { name: "alice", score: 42, extra: true }
puts(describe(p))  # output: alice 42
