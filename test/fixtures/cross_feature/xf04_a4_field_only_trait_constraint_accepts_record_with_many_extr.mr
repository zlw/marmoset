# XF04: A4: Structural shape constraint accepts record with many extra fields
shape Named = {
  name: Str
}
fn get_name[t: Named](x: t) -> Str = {
  x.name
}
let p = { name: "alice", age: 30, active: true, role: "admin", score: 100 }
puts(get_name(p))  # output: alice
