# XF04: A4: Field-only trait constraint accepts record with many extra fields
trait named {
  name: string
}
let get_name = fn[t: named](x: t) -> string {
  x.name
}
let p = { name: "alice", age: 30, active: true, role: "admin", score: 100 }
puts(get_name(p))  # output: alice
