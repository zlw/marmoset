# XF06: B6: Enum variant wrapping a record, field access after match
type Point = { x: Int, y: Int }
enum Shape = { Pt(Point) }
let p = {x: 10, y: 20}
let s = Shape.Pt(p)
match s {  # output: 10
  case Shape.Pt(q): puts(q.x)
}
