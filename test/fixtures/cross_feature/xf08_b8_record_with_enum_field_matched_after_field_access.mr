# XF08: B8: Record with enum field matched after field access
enum Status = { Ok, Fail }
let rec = { name: "test", status: Status.Ok }
let result = match rec.status {
  case Status.Ok: "found"
  case Status.Fail: "missing"
}
puts(result)  # output: found
