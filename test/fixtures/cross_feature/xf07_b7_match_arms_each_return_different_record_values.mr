# XF07: B7: Match arms each return different record values
type Direction = { Left, Right }
let d = Direction.Left
let r = match d {
  case Direction.Left: { val: 100 }
  case Direction.Right: { val: 200 }
}
puts(r.val)  # output: 100
