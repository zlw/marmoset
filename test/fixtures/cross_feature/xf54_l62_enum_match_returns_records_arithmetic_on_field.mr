# XF54: L62: Enum match returns records, arithmetic on field
enum Flag = { On, Off }
let result = match Flag.On {
  case Flag.On: { val: 100 }
  case Flag.Off: { val: 0 }
}
puts(result.val + 1)  # output: 101
