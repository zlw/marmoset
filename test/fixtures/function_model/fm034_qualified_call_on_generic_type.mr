type Opt[a] = { Some(a), None }

impl[a] Opt[a] = {
  fn is_some(o: Opt[a]) -> Bool = match o {
    case Opt.Some(_): true
    case Opt.None: false
  }
}

let x = Opt.Some(42)
puts(Opt.is_some(x))  # output: true
