type Opt[a] = { Some(a), None }

impl[a] Opt[a] = {
  fn and_then[b](o: Opt[a], f: (a) -> Opt[b]) -> Opt[b] = match o {
    case Opt.Some(v): f(v)
    case Opt.None: Opt.None
  }
}

let x: Opt[Int] = Opt.Some(42)
let y: Opt[Str] = Opt.and_then(x, (n: Int) -> Opt.Some(Show.show(n)))
