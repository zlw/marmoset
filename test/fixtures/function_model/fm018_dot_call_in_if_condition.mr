type Opt[a] = { Some(a), None }

impl[a] Opt[a] = {
  fn has(o: Opt[a]) -> Bool = match o {
    case Opt.Some(_): true
    case Opt.None: false
  }
}

let x = Opt.Some(1)
let msg = if (x.has()) { "yes" } else { "no" }
puts(msg)  # output: yes
