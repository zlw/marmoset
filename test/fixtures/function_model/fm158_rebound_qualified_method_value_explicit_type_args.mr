type Box = { v: Int }

impl Box = {
  fn cast[b](bx: Box, f: (Int) -> b) -> b = f(bx.v)
}

let cast = Box.cast
let b: Box = { v: 5 }
let s: Str = cast[Str](b, (n: Int) -> Show.show(n))
puts(s)  # output: 5
