type Val = { v: Int }

impl Val = {
  fn then[b](x: Val, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let v = {v: 3}

# chain: then returns int, then show
let result = v.then((n: Int) -> n * 10).show()
puts(result)  # output: 30
