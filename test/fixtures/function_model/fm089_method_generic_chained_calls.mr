type val = { v: int }

impl val {
  fn then[b](x: val, f: fn(int) -> b) -> b {
    f(x.v)
  }
}

let v: val = { v: 3 }
# chain: then returns int, then show
let result = v.then(fn(n: int) -> int { n * 10 }).show()
puts(result)  # output: 30
