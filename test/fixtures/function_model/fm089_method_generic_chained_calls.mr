type Val = { v: Int }

impl Val = {
  fn then[b](x: Val, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let v: Val = {v: 3}

# chain: then returns int, then show
let result = Show.show(Val.then(v, (n: Int) -> n * 10))
puts(result)  # output: 30
