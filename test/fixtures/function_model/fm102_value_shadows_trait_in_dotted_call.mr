trait Render[a] = {
  fn draw(x: a) -> Str
}

impl Render[Int] = {
  fn draw(x: Int) -> Str = { Show.show(x) }
}

let render = { draw: (x: Int) -> "custom:" + Show.show(x) }
# `render` is now a value → field-function path, not trait qualifier
puts(render.draw(42))  # output: custom:42
