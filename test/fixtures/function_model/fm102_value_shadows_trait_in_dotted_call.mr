trait render[a] {
  fn draw(x: a) -> string
}

impl render for int {
  fn draw(x: int) -> string { x.show() }
}

let render = { draw: fn(x: int) -> string { "custom:" + x.show() } }
# `render` is now a value → field-function path, not trait qualifier
puts(render.draw(42))  # output: custom:42
