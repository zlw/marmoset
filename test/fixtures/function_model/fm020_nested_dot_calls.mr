type Inner = { v: Int }
type Outer = { i: Inner }

impl Inner = {
  fn val(x: Inner) -> Int = {
    x.v
  }
}

impl Outer = {
  fn get_inner(o: Outer) -> Inner = {
    o.i
  }
}

let i = {v: 77}

let o = {i: i}

let got = Outer.get_inner(o)
puts(Inner.val(got))  # output: 77
