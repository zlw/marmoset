type Item = { name: Str }

impl Item = {
  fn describe(i: Item) -> Str = {
    "item:" + i.name
  }
}

let i = {name: "sword"}

# field access
puts(i.name)  # output: sword
# method call
puts(Item.describe(i))  # output: item:sword
