type Box = { v: Int }

trait Special[a] = {
  fn special_op(x: a) -> Int
}

impl Box = {
  fn needs_special[t: Special](b: Box, f: (Int) -> t) -> Int = {
    f(b.v).special_op()
  }
}

# Int doesn't implement Special → error
let b = {v: 1}

b.needs_special((n: Int) -> n)  # error: *
