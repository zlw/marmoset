type Val = { n: Int }

trait ViewA[a] = {
  fn view(x: a) -> Str
}

trait ViewB[a] = {
  fn view(x: a) -> Str
}

impl ViewA[Val] = {
  fn view(x: Val) -> Str = { "a" }
}

impl ViewB[Val] = {
  fn view(x: Val) -> Str = { "b" }
}

let v: Val = { n: 1 }
v.view()  # error: Ambiguous
