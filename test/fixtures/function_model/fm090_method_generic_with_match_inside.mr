type Opt[a] = { Some(a), None }

impl[a] Opt[a] = {
  fn flat_map[b](o: Opt[a], f: (a) -> Opt[b]) -> Opt[b] = match o {
    case Opt.Some(v): f(v)
    case Opt.None: Opt.None
  }
}

let x = Opt.Some(5)
let y = x.flat_map((n: Int) -> {
  if (n > 0) { Opt.Some(n.show()) } else { Opt.None }
})
