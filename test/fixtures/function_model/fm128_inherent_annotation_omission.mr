type Point = { x: Int, y: Int }

# Inherent method omits return annotation — infers from body
impl Point = {
  fn sum(p: Point) = {
    p.x + p.y
  }
}

let p: Point = { x: 3, y: 7 }
puts(p.sum())  # output: 10
