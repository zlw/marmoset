type Vec2 = { x: Int, y: Int }

impl Vec2 = {
  fn mag_sq(v: Vec2) -> Int = {
    v.x * v.x + v.y * v.y
  }
  fn sum(v: Vec2) -> Int = {
    v.x + v.y
  }
}

let v = {x: 3, y: 4}

puts(Vec2.mag_sq(v))  # output: 25
puts(Vec2.sum(v))  # output: 7
