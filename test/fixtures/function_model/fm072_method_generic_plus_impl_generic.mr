type Result[a, b] = { Ok(a), Err(b) }

impl[a, b] Result[a, b] = {
  fn map[c](r: Result[a, b], f: (a) -> c) -> Result[c, b] = match r {
    case Result.Ok(v): Result.Ok(f(v))
    case Result.Err(e): Result.Err(e)
  }
}

let r = Result.Ok(42)
let r2 = r.map((n: Int) -> n.show())
