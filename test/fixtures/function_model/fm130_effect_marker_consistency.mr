trait Processor[a] = {
  fn process(x: a) => Str
}

type Data = { v: Int }

impl Processor[Data] = {
  fn process(x: Data) => Str = {
    Show.show(x.v)
  }
}

let d = {v: 7}

puts(Processor.process(d))  # output: 7
