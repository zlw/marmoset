trait Processor[a] = {
  fn process(x: a) => Str
}

type Data = { v: Int }

impl Processor[Data] = {
  fn process(x: Data) => Str = {
    x.v.show()
  }
}

let d: Data = { v: 7 }
puts(d.process())  # output: 7
