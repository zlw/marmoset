# xfail: method-level generics not implemented
type config = { value: int }

impl config {
  fn map_value[b](c: config, f: fn(int) -> b) -> b {
    f(c.value)
  }
}

let cfg: config = { value: 99 }
puts(cfg.map_value(fn(n: int) -> string { n.show() }))  # output: 99
