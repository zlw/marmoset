type Config = { value: Int }

impl Config = {
  fn map_value[b](c: Config, f: (Int) -> b) -> b = {
    f(c.value)
  }
}

let cfg: Config = {value: 99}

puts(Config.map_value(cfg, (n: Int) -> Show.show(n)))  # output: 99
