type Config = { value: Int }

impl Config = {
  fn map_value[b](c: Config, f: (Int) -> b) -> b = {
    f(c.value)
  }
}

let cfg = {value: 99}

puts(cfg.map_value((n: Int) -> n.show()))  # output: 99
