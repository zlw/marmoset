type Big = { a: Int, b: Int, c: Int }

impl Big = {
  fn total(b: Big) -> Int = { b.a + b.b + b.c }
  fn first(b: Big) -> Int = { b.a }
  fn last(b: Big) -> Int = { b.c }
}

let x = {a: 1, b: 2, c: 3}

# only use one method
puts(x.total())  # output: 6
