type Flag = { on: Bool }

impl Flag = {
  fn is_on(f: Flag) -> Bool = {
    f.on
  }
}

let f: Flag = { on: true }
let msg = match Flag.is_on(f) {
  case true: "yes"
  case false: "no"
}
puts(msg)  # output: yes
