enum Thing = { Value(Int) }

impl Thing = {
  fn value(t: Thing) -> Int = match t {
    case Thing.Value(n): n
  }
}

# On enum qualifier: variant constructor wins
let t = Thing.Value(42)
# On type qualifier: inherent method
puts(Thing.value(t))  # output: 42
