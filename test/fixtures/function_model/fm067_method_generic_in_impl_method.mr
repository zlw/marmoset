type Box = { v: Int }

trait Mapper[a] = {
  fn map[b](x: a, f: (Int) -> b) -> b
}

impl Mapper[Box] = {
  fn map[b](x: Box, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let b = {v: 42}

let s = b.map((n: Int) -> n.show())
puts(s)  # output: 42
