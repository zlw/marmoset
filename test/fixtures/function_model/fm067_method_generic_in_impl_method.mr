type Box = { v: Int }

trait Mapper[a] = {
  fn map[b](x: a, f: (Int) -> b) -> b
}

impl Mapper[Box] = {
  fn map[b](x: Box, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let b = {v: 42}

let s = Mapper.map(b, (n: Int) -> Show.show(n))
puts(s)  # output: 42
