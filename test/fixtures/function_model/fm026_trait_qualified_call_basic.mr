type Score = { v: Int }

trait Display[a] = {
  fn render(x: a) -> Str
}

impl Display[Score] = {
  fn render(x: Score) -> Str = {
    "score:" + x.v.show()
  }
}

let s = Score(v: 100)

puts(Display.render(s))  # output: score:100
