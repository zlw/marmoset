type score = { v: int }

trait display[a] {
  fn render(x: a) -> string
}

impl display for score {
  fn render(x: score) -> string {
    "score:" + x.v.show()
  }
}

let s: score = { v: 100 }
puts(display.render(s))  # output: score:100
