type Score = { v: Int }

trait Display[a] = {
  fn render(x: a) -> Str
}

impl Display[Score] = {
  fn render(x: Score) -> Str = {
    "score:" + Show.show(x.v)
  }
}

let s = {v: 100}

puts(Display.render(s))  # output: score:100
