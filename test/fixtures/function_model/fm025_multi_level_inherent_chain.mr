type Level = { n: Int }

impl Level = {
  fn next(l: Level) -> Level = {
    {n: l.n + 1}

  }
  fn value(l: Level) -> Int = {
    l.n
  }
}

let l = {n: 0}

puts(Level.value(Level.next(Level.next(Level.next(l)))))  # output: 3
