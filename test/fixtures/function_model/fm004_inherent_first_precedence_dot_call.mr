type Widget = { v: Int }

trait Describe[a] = { fn info(x: a) -> Str }

impl Describe[Widget] = {
  fn info(x: Widget) -> Str = {
    "trait:" + x.v.show()
  }
}

impl Widget = {
  fn info(w: Widget) -> Str = {
    "inherent:" + w.v.show()
  }
}

let w = {v: 42}

puts(Describe.info(w))  # output: trait:42
puts(Widget.info(w))  # output: inherent:42
