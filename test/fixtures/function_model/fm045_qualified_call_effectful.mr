type Logger = { prefix: Str }

impl Logger = {
  fn log(l: Logger, msg: Str) => Str = {
    l.prefix + ": " + msg
  }
}

let l = Logger(prefix: "INFO")

puts(Logger.log(l, "hello"))  # output: INFO: hello
