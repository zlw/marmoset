trait Display[a] = {
  fn render(x: a) -> Str
}

type Thing = { v: Int }

# Impl omits parameter/return annotations — infers from trait sig
impl Display[Thing] = {
  fn render(x) = {
    "thing:" + Show.show(x.v)
  }
}

let t = {v: 42}

puts(Display.render(t))  # output: thing:42
