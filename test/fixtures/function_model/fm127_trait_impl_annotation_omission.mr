trait Display[a] = {
  fn render(x: a) -> Str
}

type Thing = { v: Int }

# Impl omits parameter/return annotations — infers from trait sig
impl Display[Thing] = {
  fn render(x) = {
    "thing:" + x.v.show()
  }
}

let t = Thing(v: 42)

puts(t.render())  # output: thing:42
