type Box = { v: Int }

impl Box = {
  fn transform[b](bx: Box, f: (Int) -> b) -> b = {
    f(bx.v)
  }
}

let b = Box(v: 10)

# inferred
let r1 = b.transform((n: Int) -> n.show())
# explicit
let r2 = b.transform[Int]((n: Int) -> n + 1)
puts(r1)  # output: 10
puts(r2)  # output: 11
