type Wrapper = { v: Int }

impl Wrapper = {
  fn transform[b](w: Wrapper, f: (Int) -> b) -> b = {
    f(w.v)
  }
}

let w: Wrapper = {v: 10}

let s = Wrapper.transform(w, (n: Int) -> Show.show(n))
puts(s)  # output: 10
