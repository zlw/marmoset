type box = { v: int }

impl box {
  fn cast[b](bx: box, f: fn(int) -> b) -> b {
    f(bx.v)
  }
}

let b: box = { v: 1 }
# cast has 1 type param, providing 2
b.cast[int, string](fn(n: int) -> int { n })  # error: *
