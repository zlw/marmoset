type Box = { v: Int }

impl Box = {
  fn cast[b](bx: Box, f: (Int) -> b) -> b = {
    f(bx.v)
  }
}

let b = Box(v: 1)

# cast has 1 type param, providing 2
b.cast[Int, Str]((n: Int) -> n)  # error: *
