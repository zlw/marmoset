type Maybe[a] = { Just(a), Nothing }

impl[a] Maybe[a] = {
  fn is_just(m: Maybe[a]) -> Bool = match m {
    case Maybe.Just(_): true
    case Maybe.Nothing: false
  }
}

let x = Maybe.Just(42)
puts(x.is_just())  # output: true
let y: Maybe[Int] = Maybe.Nothing
puts(y.is_just())  # output: false
