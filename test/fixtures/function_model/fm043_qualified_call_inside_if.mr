type Toggle = { active: Bool }

impl Toggle = {
  fn check(t: Toggle) -> Bool = {
    t.active
  }
}

let t = Toggle(active: false)

let result = if (Toggle.check(t)) { "on" } else { "off" }
puts(result)  # output: off
