type Box = { v: Int }

impl Box = {
  fn cast[b](bx: Box, f: (Int) -> b) -> b = {
    f(bx.v)
  }
}

let b: Box = {v: 5}

fn show_int(n: Int) -> Str = Show.show(n)

# method type args: .cast[Str](...) → method call with type arg
let s: Str = Box.cast[Str](b, show_int)
puts(s)  # output: 5
