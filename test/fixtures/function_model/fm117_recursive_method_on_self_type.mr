type Counter = { n: Int }

impl Counter = {
  fn count_down(c: Counter) -> Str = {
    if (c.n <= 0) {
      "done"
    } else {
      let next = {n: c.n - 1}

      Counter.count_down(next)
    }
  }
}

let c = {n: 3}

puts(Counter.count_down(c))  # output: done
