type Counter = { n: Int }

impl Counter = {
  fn count_down(c: Counter) -> Str = {
    if (c.n <= 0) {
      "done"
    } else {
      let next = Counter(n: c.n - 1)

      next.count_down()
    }
  }
}

let c = Counter(n: 3)

puts(c.count_down())  # output: done
