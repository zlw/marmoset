enum Shape = { Circle(Int), Square(Int) }

let c = Shape.Circle(5)
let result = match c {
  case Shape.Circle(r): "circle:" + r.show()
  case Shape.Square(s): "square:" + s.show()
}
puts(result)  # output: circle:5
