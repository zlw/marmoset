trait EmptyTrait[a] = {
  fn exists(x: a) -> Bool
}

type Thing = { v: Int }

impl EmptyTrait[Thing] = {
  fn exists(x: Thing) -> Bool = { true }
}

let t = {v: 0}

EmptyTrait.nonexistent(t)  # error: *
