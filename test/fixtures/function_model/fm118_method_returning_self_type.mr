type Builder = { parts: Int }

impl Builder = {
  fn add(b: Builder) -> Builder = {
    {parts: b.parts + 1}

  }
  fn count(b: Builder) -> Int = {
    b.parts
  }
}

let b = {parts: 0}

puts(Builder.count(Builder.add(Builder.add(Builder.add(b)))))  # output: 3
