type Tagged = { tag: Int }

impl Tagged = {
  fn get_tag(t: Tagged) -> Int = {
    t.tag
  }
}

let t = {tag: 2}

let result = match Tagged.get_tag(t) {
  case 1: "one"
  case 2: "two"
  case _: "other"
}
puts(result)  # output: two
