type Tagged = { tag: Int }

impl Tagged = {
  fn get_tag(t: Tagged) -> Int = {
    t.tag
  }
}

let t: Tagged = { tag: 2 }
let result = match t.get_tag() {
  case 1: "one"
  case 2: "two"
  case _: "other"
}
puts(result)  # output: two
