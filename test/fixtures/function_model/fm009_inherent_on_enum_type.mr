enum Box = { Val(Int), Empty }

impl Box = {
  fn is_empty(b: Box) -> Bool = match b {
    case Box.Empty: true
    case Box.Val(_): false
  }
}

let b = Box.Val(42)
puts(b.is_empty())  # output: false
let e = Box.Empty
puts(e.is_empty())  # output: true
