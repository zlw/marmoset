enum Box = { Val(Int), Empty }

impl Box = {
  fn is_empty(b: Box) -> Bool = match b {
    case Box.Empty: true
    case Box.Val(_): false
  }
}

let b = Box.Val(42)
puts(Box.is_empty(b))  # output: false
let e = Box.Empty
puts(Box.is_empty(e))  # output: true
