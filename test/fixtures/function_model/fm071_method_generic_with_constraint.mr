type Container = { v: Int }

impl Container = {
  fn show_as[b: Show](c: Container, f: (Int) -> b) -> Str = {
    f(c.v).show()
  }
}

let c = Container(v: 7)

puts(c.show_as((n: Int) -> n + 1))  # output: 8
