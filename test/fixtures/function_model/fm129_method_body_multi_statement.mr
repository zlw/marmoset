type Acc = { total: Int }

impl Acc = {
  fn add_and_show(a: Acc, n: Int) -> Str = {
    let new_total = a.total + n
    let result = "total:" + new_total.show()
    result
  }
}

let a: Acc = { total: 10 }
puts(a.add_and_show(5))  # output: total:15
