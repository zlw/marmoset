type Num = { v: Int }

impl Num = {
  fn inc(n: Num) -> Num = {
    Num(v: n.v + 1)

  }
  fn value(n: Num) -> Int = {
    n.v
  }
}

let n = Num(v: 0)

# mix qualified and dot forms
let n2 = Num.inc(n)
puts(n2.value())  # output: 1
