type Num = { v: Int }

impl Num = {
  fn inc(n: Num) -> Num = {
    {v: n.v + 1}

  }
  fn value(n: Num) -> Int = {
    n.v
  }
}

let n = {v: 0}

# mix qualified and dot forms
let n2 = Num.inc(n)
let value = Num.value
puts(value(n2))  # output: 1
