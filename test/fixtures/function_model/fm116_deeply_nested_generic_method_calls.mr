type Val = { v: Int }

impl Val = {
  fn apply[b](x: Val, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let v: Val = {v: 2}

fn times_ten(n: Int) -> Int = n * 10

# Deeply nested: apply(apply(apply(...)))
let r: Int = Val.apply(v, times_ten)
puts(Show.show(r))  # output: 20
