type val = { v: int }

impl val {
  fn apply[b](x: val, f: fn(int) -> b) -> b {
    f(x.v)
  }
}

let v: val = { v: 2 }
# Deeply nested: apply(apply(apply(...)))
let r = v.apply(fn(n: int) -> int { n * 10 })
puts(r.show())  # output: 20
