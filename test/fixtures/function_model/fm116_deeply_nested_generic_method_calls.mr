type Val = { v: Int }

impl Val = {
  fn apply[b](x: Val, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let v = {v: 2}

# Deeply nested: apply(apply(apply(...)))
let r = v.apply((n: Int) -> n * 10)
puts(r.show())  # output: 20
