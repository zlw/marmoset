type Val = { v: Int }

trait Reader[a] = {
  fn read(x: a) -> Int
}

trait Sensor[a] = {
  fn read(x: a) -> Int
}

impl Reader[Val] = {
  fn read(x: Val) -> Int = { x.v }
}

impl Sensor[Val] = {
  fn read(x: Val) -> Int = { x.v + 1 }
}

let v = Val(v: 5)

v.read()  # error: Ambiguous
