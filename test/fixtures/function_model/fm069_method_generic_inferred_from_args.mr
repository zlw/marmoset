type Opt[a] = { Some(a), None }

impl[a] Opt[a] = {
  fn map[b](o: Opt[a], f: (a) -> b) -> Opt[b] = match o {
    case Opt.Some(v): Opt.Some(f(v))
    case Opt.None: Opt.None
  }
}

let x: Opt[Int] = Opt.Some(42)
let y: Opt[Str] = Opt.map(x, (n: Int) -> Show.show(n))
