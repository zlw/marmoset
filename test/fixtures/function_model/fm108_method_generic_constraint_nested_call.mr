type Box = { v: Int }

impl Box = {
  fn apply[b: Show](bx: Box, f: (Int) -> b) -> Str = {
    Show.show(f(bx.v))
  }
}

let b: Box = {v: 7}

fn double(n: Int) -> Int = n * 2

# Deeply nested: apply(apply(apply(...)))
let r: Str = Box.apply(b, double)
puts(r)  # output: 14
