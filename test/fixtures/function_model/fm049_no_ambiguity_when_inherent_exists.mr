type Cell = { v: Int }

trait Render[a] = {
  fn draw(x: a) -> Str
}

impl Render[Cell] = {
  fn draw(x: Cell) -> Str = { "trait" }
}

impl Cell = {
  fn draw(c: Cell) -> Str = { "inherent" }
}

let c: Cell = { v: 1 }
# inherent wins, no ambiguity
puts(c.draw())  # output: inherent
