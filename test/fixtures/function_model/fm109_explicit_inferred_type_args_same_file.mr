type Val = { v: Int }

impl Val = {
  fn convert[b](x: Val, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let v = {v: 42}

let inferred = v.convert((n: Int) -> n.show())
let explicit = v.convert[Bool]((n: Int) -> n > 0)
puts(inferred)  # output: 42
puts(explicit)  # output: true
