type Color = { Red, Green, Blue }

trait Label[a] = { fn label(x: a) -> Str }

impl Label[Color] = {
  fn label(x: Color) -> Str = match x {
    case Color.Red: "red"
    case Color.Green: "green"
    case Color.Blue: "blue"
  }
}

let c = Color.Red
puts(c.label())  # output: red
