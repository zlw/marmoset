enum Wrap[a] = { Val(a) }

impl Wrap[Int] = {
  fn get(w: Wrap[Int]) -> Int = match w {
    case Wrap.Val(n): n
  }
}

impl[a] Wrap[a] = {
  fn get(w: Wrap[a]) -> a = match w {
    case Wrap.Val(v): v
  }
}

let w = Wrap.Val(42)
puts(w.get())  # output: 42
