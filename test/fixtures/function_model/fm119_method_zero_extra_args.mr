type Flag = { on: Bool }

impl Flag = {
  fn toggle(f: Flag) -> Flag = {
    if (f.on) {
      Flag(on: false)
    } else {
      Flag(on: true)
    }
  }
}

let f = Flag(on: true)
let f2 = f.toggle()
puts(f2.on)  # output: false
