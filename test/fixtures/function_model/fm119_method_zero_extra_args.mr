type Flag = { on: Bool }

impl Flag = {
  fn toggle(f: Flag) -> Flag = {
    if (f.on) {
      {on: false}
    } else {
      {on: true}
    }
  }
}

let f = {on: true}
let f2 = Flag.toggle(f)
puts(f2.on)  # output: false
