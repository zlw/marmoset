type Item = { v: Int }

trait Alpha[a] = {
  fn tag(x: a) -> Str
}

trait Beta[a] = {
  fn tag(x: a) -> Str
}

impl Alpha[Item] = {
  fn tag(x: Item) -> Str = { "a" }
}

impl Beta[Item] = {
  fn tag(x: Item) -> Str = { "b" }
}

let i = Item(v: 1)

i.tag()  # error: Ambiguous
