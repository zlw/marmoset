# xfail: value-shadows-enum classification not implemented
enum color { red green blue }

let color = { red: "my_red" }
# `color` is now a value (record), not the enum
puts(color.red)  # output: my_red
