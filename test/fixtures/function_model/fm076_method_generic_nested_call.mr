type Val = { v: Int }

impl Val = {
  fn apply[b](x: Val, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let v = Val(v: 5)

# nested: apply returns int, then show on it
puts(v.apply((n: Int) -> n * 3).show())  # output: 15
