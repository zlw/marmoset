type Val = { v: Int }

impl Val = {
  fn apply[b](x: Val, f: (Int) -> b) -> b = {
    f(x.v)
  }
}

let v: Val = {v: 5}

# nested: apply returns int, then show on it
puts(Show.show(Val.apply(v, (n: Int) -> n * 3)))  # output: 15
