enum Wrap[a] = { Val(a) }

impl[a] Wrap[a] = {
  fn convert[b](w: Wrap[a], f: (a) -> b) -> Wrap[b] = match w {
    case Wrap.Val(v): Wrap.Val(f(v))
  }
}

# a and b must be separate scopes — no leakage
let w = Wrap.Val(42)
let w2 = w.convert((n: Int) -> n.show())
