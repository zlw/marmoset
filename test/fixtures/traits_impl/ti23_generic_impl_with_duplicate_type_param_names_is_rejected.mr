# TI23: Generic impl with duplicate type param names is rejected
trait Show[a] = {  # error: duplicate type parameter
  fn show(x: a) -> Str
}
impl[b: Show, b: Show] Show[List[b]] = {
  fn show(x: List[b]) -> Str = "ok"
}
puts(Show.show([1]))
