# TI35: Constrained generic fn calls trait method from generic impl
impl[b: Show] Show[List[b]] = {
  fn show(x: List[b]) -> Str = "ok"
}
fn render[t: Show](x: t) -> Str = {
  Show.show(x)
}
puts(render([1, 2]))  # output: ok
