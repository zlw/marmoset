# TI09: Impl method returns different type than receiver
type Pair = { a: Int, b: Int }
trait Sum[a] = {
  fn sum(x: a) -> Int
}
impl Sum[Pair] = {
  fn sum(p: Pair) -> Int = {
    p.a + p.b
  }
}
let p: Pair = {a: 1, b: 2}
puts(Sum.sum(p))  # output: 3
