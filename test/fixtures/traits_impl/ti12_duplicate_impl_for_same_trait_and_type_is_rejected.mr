# TI12: Duplicate impl for same trait and type is rejected
trait Show[a] = {
  fn show(x: a) -> Str
}
impl Show[Int] = {
  fn show(x: Int) -> Str = "first"
}
impl Show[Int] = {  # error: Duplicate impl registration for trait 'show' and type Int
  fn show(x: Int) -> Str = "second"
}
puts(1)
