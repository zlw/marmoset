# TI01: Impl for record type via type alias
type Point = { x: Int, y: Int }
trait Show[a] = {
  fn show(x: a) -> Str
}
impl Show[Point] = {
  fn show(p: Point) -> Str = {
    "point(" + p.x.show() + "," + p.y.show() + ")"
  }
}
let p: Point = { x: 1, y: 2 }
puts(p.show())  # output: point(1,2)
