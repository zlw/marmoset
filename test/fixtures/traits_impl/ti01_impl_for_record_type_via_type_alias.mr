# TI01: Impl for record type via transparent type
type Point = { x: Int, y: Int }
impl Show[Point] = {
  fn show(p: Point) -> Str = {
    "point(" + Show.show(p.x) + "," + Show.show(p.y) + ")"
  }
}
let p: Point = {x: 1, y: 2}
puts(Show.show(p))  # output: point(1,2)
