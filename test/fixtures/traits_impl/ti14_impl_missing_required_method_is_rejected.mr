# TI14: Impl missing required method is rejected
# error: does not match trait signature
trait dialog[a] {
  fn hello(x: a) -> string
  fn goodbye(x: a) -> string
}
impl dialog for int {
  fn hello(x: int) -> string { "hi" }
}
puts(1)
