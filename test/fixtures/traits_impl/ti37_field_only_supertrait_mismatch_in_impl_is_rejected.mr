# TI37: Shape superconstraints are enforced for trait impl targets
shape Named = { name: Str }

trait Shown[a]: Named = {
  fn show(x: a) -> Str
}

impl Shown[Int] = {  # error: required supertrait
  fn show(x: Int) -> Str = x.show()
}

puts(1)
