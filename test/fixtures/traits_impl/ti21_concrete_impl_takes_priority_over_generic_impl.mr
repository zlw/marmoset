# TI21: Concrete impl takes priority over generic impl
trait Label[a] = {
  fn label(x: a) -> Str
}
impl[b: Show] Label[List[b]] = {
  fn label(x: List[b]) -> Str = "generic"
}
impl Label[List[Int]] = {
  fn label(x: List[Int]) -> Str = "concrete"
}
puts(Label.label([1, 2, 3]))  # output: concrete
