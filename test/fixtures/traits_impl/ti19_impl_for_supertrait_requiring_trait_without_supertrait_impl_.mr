# TI19: Impl for supertrait-requiring trait without supertrait impl is rejected
trait Eq[a] = {  # error: supertrait
  fn eq(x: a, y: a) -> Bool
}
trait Ord[a]: Eq = {
  fn compare(x: a, y: a) -> Int
}
type Foo = { x: Int }
impl Ord[Foo] = {
  fn compare(a: Foo, b: Foo) -> Int = 0
}
puts(1)
