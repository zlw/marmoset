# TI20: Generic impl with single type param
trait show[a] {
  fn show(x: a) -> string
}
impl show[b: show] for list[b] {
  fn show(x: list[b]) -> string { "list" }
}
puts([1, 2, 3].show())  # output: list
