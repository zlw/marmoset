# TI10: Trait method on type without impl fails
trait Describe[a] = {
  fn describe(x: a) -> Str
}
impl Describe[Int] = {
  fn describe(x: Int) -> Str = "int"
}
puts("hello".describe())  # error: No method 'describe' found for type
