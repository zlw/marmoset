# TI15: Impl with extra method not in trait is rejected
trait Show[a] = {  # error: provides methods not in trait: extra
  fn show(x: a) -> Str
}
impl Show[Int] = {
  fn show(x: Int) -> Str = "int"
  fn extra(x: Int) -> Str = "bonus"
}
puts(1)
