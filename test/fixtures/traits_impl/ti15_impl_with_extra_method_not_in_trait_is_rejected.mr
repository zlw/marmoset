# TI15: Impl with extra method not in trait is rejected
trait show[a] {  # error: does not match trait signature
  fn show(x: a) -> string
}
impl show for int {
  fn show(x: int) -> string { "int" }
  fn extra(x: int) -> string { "bonus" }
}
puts(1)
