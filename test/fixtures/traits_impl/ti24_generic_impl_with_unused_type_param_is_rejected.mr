# TI24: Generic impl with unused type param is rejected
trait Show[a] = {  # error: not used in impl target type
  fn show(x: a) -> Str
}
impl[b: Show] Show[Int] = {
  fn show(x: Int) -> Str = "int"
}
puts(1)
