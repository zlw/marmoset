# TI08: Impl with multiple methods
trait Dialog[a] = {
  fn hello(x: a) -> Str
  fn goodbye(x: a) -> Str
}
impl Dialog[Int] = {
  fn hello(x: Int) -> Str = "hello"
  fn goodbye(x: Int) -> Str = "goodbye"
}
puts(1.hello())  # output: hello
puts(1.goodbye())  # output: goodbye
