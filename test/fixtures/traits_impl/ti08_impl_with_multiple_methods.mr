# TI08: Impl with multiple methods
trait Dialog[a] = {
  fn hello(x: a) -> Str
  fn goodbye(x: a) -> Str
}
impl Dialog[Int] = {
  fn hello(x: Int) -> Str = "hello"
  fn goodbye(x: Int) -> Str = "goodbye"
}
puts(Dialog.hello(1))  # output: hello
puts(Dialog.goodbye(1))  # output: goodbye
