# TI06: Impl method accesses receiver fields
type Point = { x: Int, y: Int }
trait SumFields[a] = {
  fn sum_fields(x: a) -> Int
}
impl SumFields[Point] = {
  fn sum_fields(p: Point) -> Int = {
    p.x + p.y
  }
}
let p: Point = { x: 1, y: 2 }
puts(p.sum_fields())  # output: 3
