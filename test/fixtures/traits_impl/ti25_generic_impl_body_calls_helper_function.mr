# TI25: Generic impl body calls helper function
trait Size[a] = {
  fn size(x: a) -> Int
}
impl[b: Show] Size[List[b]] = {
  fn size(x: List[b]) -> Int = len(x)
}
puts(Size.size([10, 20]))  # output: 2
