# TI22: Generic impl constraint enforced: element must satisfy constraint
trait named {  # error: does not satisfy
  name: string
}
trait show[a] {
  fn show(x: a) -> string
}
impl show[b: named] for list[b] {
  fn show(x: list[b]) -> string { "ok" }
}
puts([1, 2].show())
