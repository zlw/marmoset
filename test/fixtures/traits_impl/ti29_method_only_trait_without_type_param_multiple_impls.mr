# TI29: Method-only trait without type param, multiple impls
trait Ping = {
  fn ping(x: Int) -> Int
}
impl Ping[Int] = {
  fn ping(x: Int) -> Int = x * 10
}
puts(Ping.ping(1))  # output: 10
puts(Ping.ping(2))  # output: 20
