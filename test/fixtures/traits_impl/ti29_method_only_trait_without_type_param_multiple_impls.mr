# TI29: Method-only trait without type param, multiple impls
trait Ping = {
  fn ping(x: Int) -> Int
}
impl Ping[Int] = {
  fn ping(x: Int) -> Int = x * 10
}
puts(1.ping())  # output: 10
puts(2.ping())  # output: 20
