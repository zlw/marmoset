# TI10: Trait method on type without impl fails
# error: No method 'describe' found for type
trait describe[a] {
  fn describe(x: a) -> string
}
impl describe for int {
  fn describe(x: int) -> string { "int" }
}
puts("hello".describe())
