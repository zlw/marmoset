# TI30: Two traits same method name, only one impl means no ambiguity
trait AlphaTrait[a] = {
  fn tag(x: a) -> Str
}
trait BetaTrait[a] = {
  fn tag(x: a) -> Str
}
impl AlphaTrait[Int] = {
  fn tag(x: Int) -> Str = "alpha"
}
puts(1.tag())  # output: alpha
