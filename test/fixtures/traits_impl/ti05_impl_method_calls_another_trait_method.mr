# TI05: Impl method calls another trait method
type Box = { value: Int }
trait Describe[a] = {
  fn describe(x: a) -> Str
}
impl Describe[Box] = {
  fn describe(b: Box) -> Str = {
    "value:" + Show.show(b.value)
  }
}
let b: Box = {value: 42}
puts(Describe.describe(b))  # output: value:42
