# TI05: Impl method calls another trait method
type Box = { value: Int }
trait Describe[a] = {
  fn describe(x: a) -> Str
}
impl Describe[Box] = {
  fn describe(b: Box) -> Str = {
    "value:" + b.value.show()
  }
}
let b: Box = {value: 42}
puts(b.describe())  # output: value:42
