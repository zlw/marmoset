# TI16: Old field-only trait syntax is rejected before impl validation
trait Named = {
  name: Str  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
impl Named[Int] = {
  fn fake(x: Int) -> Str = "nope"
}
puts(1)
