# TI33: Impl method missing param type annotation is rejected
# error: missing a type annotation
trait show[a] {
  fn show(x: a) -> string
}
impl show for int {
  fn show(x) -> string { "int" }
}
puts(1)
