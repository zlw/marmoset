# TI33: Impl method missing param type annotation is rejected
trait show[a] {  # error: missing a type annotation
  fn show(x: a) -> string
}
impl show for int {
  fn show(x) -> string { "int" }
}
puts(1)
