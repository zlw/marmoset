# TI33: Impl method param type annotation is optional (inferred from trait sig)
trait show[a] {
  fn show(x: a) -> string
}
impl show for int {
  fn show(x) -> string { "int" }
}
puts(1)  # output: 1
