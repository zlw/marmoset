let x = Color.Red
enum Color = { Red, Green, Blue }
impl Show[Color] = {
  fn show(color: Color) -> Str = match color {
    case Color.Red: "Red"
    case Color.Green: "Green"
    case Color.Blue: "Blue"
  }
}
puts(x)  # output: Red
