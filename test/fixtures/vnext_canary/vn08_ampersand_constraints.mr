# VN08: & in constraint composition (generic param bounds)
trait display[a] {
  fn display(x: a) -> string
}
fn show_it[a: display](x: a) -> string = x.display()
impl display for int {
  fn display(x: int) -> string { x.show() }
}
puts(show_it(42))  # output: 42
