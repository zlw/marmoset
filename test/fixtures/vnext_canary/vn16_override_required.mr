# VN16: Override required when replacing a trait default method
trait greeter[a] {
  fn greet(x: a) -> string = "hello"
}
impl greeter[int] = {
  fn greet(x: int) -> string = "hi"  # error: override-required
}
puts(1.greet())
