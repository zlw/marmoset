# VN30: Placeholder shorthand rewrites in method-call callback slots
type Box = { value: Int }

impl Box = {
  fn map[b](box: Box, f: (Int) -> b) -> b = f(box.value)
}

let box: Box = Box(value: 21)
puts(box.map(_ * 2))  # output: 42
