# VN88: Legacy field-style trait syntax is rejected before derive validation
trait Named = {
  name: Str  # error: Trait bodies are method-only; move structural fields to a shape declaration
}

type Person = { name: Str } derive Named
