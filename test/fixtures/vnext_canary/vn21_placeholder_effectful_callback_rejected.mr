# VN21: Placeholder shorthand stays pure-only in callback position
fn foreach_list[a](items: List[a], f: (a) => Unit) => Unit = puts("x")

foreach_list([1, 2], puts(_))  # error: effectful callbacks require explicit '=>'
