# VN32: Placeholder shorthand stays pure-only in qualified-call callback slots
type Counter = { value: Int }

impl Counter = {
  fn each(counter: Counter, f: (Int) => Unit) => Unit = f(counter.value)
}

let counter: Counter = {value: 1}
Counter.each(counter, puts(_))  # error: explicit '=>'
