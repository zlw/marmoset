# VN41: Inherent methods may omit their signature suffix and infer it from the body
type Point = { x: Int, y: Int }

impl Point = {
  fn sum(point: Point) = point.x + point.y
}

let point: Point = {x: 19, y: 23}
puts(point.sum())  # output: 42
