# VN34: Ampersand constraints compose multiple trait requirements
fn show_if_equal[a: Show & Eq](x: a, y: a) -> Str = {
  if (Eq.eq(x, y)) {
    Show.show(x)
  } else {
    "different"
  }
}

puts(show_if_equal(42, 42))  # output: 42
puts(show_if_equal(1, 2))    # output: different
