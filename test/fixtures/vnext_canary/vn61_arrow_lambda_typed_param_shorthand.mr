# VN61: Typed arrow-lambda params accept constrained-param shorthand
shape Named = { name: Str }

fn apply[a, b](x: a, f: (a) -> b) -> b = f(x)

let user = { name: "mia", id: 7 }
puts(apply(user, (x: Named) -> x.name))  # output: mia
