# VN18: override on a required (non-default) method is allowed
trait Greeter[a] = {
  fn greet(x: a) -> Str
}
impl Greeter[Int] = {
  override fn greet(x: Int) -> Str = "hi"  # warning: override-unnecessary
}
puts(1.greet())  # output: hi
