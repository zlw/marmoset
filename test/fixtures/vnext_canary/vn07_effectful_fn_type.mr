# VN07: Effectful function type annotation (Int) => Str
fn apply_effectful(f: (Int) => Str, x: Int) => Str = {
  f(x)
}
fn to_str(n: Int) => Str = n.show()
puts(apply_effectful(to_str, 42))  # output: 42
