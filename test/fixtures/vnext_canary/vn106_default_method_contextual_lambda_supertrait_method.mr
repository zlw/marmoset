# VN106: Default methods resolve supertrait methods inside explicit callback lambdas
fn apply[a, b](x: a, f: (a) -> b) -> b = f(x)

trait Fancy[a]: Show = {
  fn fancy(self: a) -> Str = apply(self, (x) -> Show.show(x))
}

impl Fancy[Int] = {}
puts(Fancy.fancy(1))  # output: 1
