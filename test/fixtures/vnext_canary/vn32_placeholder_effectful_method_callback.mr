# VN32: Placeholder shorthand infers effectful qualified-call callback slots
type Counter = { value: Int }

impl Counter = {
  fn each(counter: Counter, f: (Int) => Unit) => Unit = f(counter.value)
}

let counter: Counter = {value: 1}
Counter.each(counter, puts(_))  # output: 1
