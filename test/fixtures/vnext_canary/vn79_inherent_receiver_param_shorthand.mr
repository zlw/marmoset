# VN79: Inherent methods may type their receiver with constrained-param shorthand
trait Named = { name: Str }

type User = { name: Str, id: Int }

impl User = {
  fn label(user: Named) -> Str = user.name
}

puts({ name: "Ada", id: 1 }.label())  # output: Ada
