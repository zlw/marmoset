# VN79: Inherent methods still require the receiver to match the impl target type
type User = { name: Str, id: Int }

impl User = {
  fn label(user: User) -> Str = user.name
}

puts(User.label({name: "Ada", id: 1}))  # output: Ada
