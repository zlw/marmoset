# VN36: Pure function types work in transparent typees and annotations
type Formatter = (Int) -> Str

fn apply_formatter(f: Formatter, n: Int) -> Str = f(n)

let fmt: Formatter = (n: Int) -> "n=" + n.show()
puts(apply_formatter(fmt, 42))  # output: n=42
