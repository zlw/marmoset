# VN109: User-trait derives can rely on later explicit supertrait impls
trait Base[a] = {
  fn base(self: a) -> Str = "base"
}

trait Child[a]: Base = {
  fn child(self: a) -> Str = Base.base(self) + ":child"
}

type Score = Int derive Child

impl Base[Score] = {}

let score: Score = 1
puts(Child.child(score))  # output: base:child
