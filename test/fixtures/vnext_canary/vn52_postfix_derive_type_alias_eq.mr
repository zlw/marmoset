# VN52: Postfix derive on type aliases lowers to canonical derive obligations
type Point = { x: Int, y: Int } derive Eq

let a: Point = Point(x: 1, y: 2)
let b: Point = Point(x: 1, y: 2)

puts(a.eq(b))  # output: true
