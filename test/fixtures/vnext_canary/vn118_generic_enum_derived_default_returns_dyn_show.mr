# VN118: Generic enum derives may return Dyn[...] from default methods
trait Boxed[a]: Show = {
  fn box(self: a) -> Dyn[Show] = self
}

enum Option[t] = {
  Some(t)
  None
} derive Show, Boxed

let value = Boxed.box(Option.Some(1))
puts(Show.show(value))  # output: Some(1)
