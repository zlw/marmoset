# VN72: Identifier suffix methods compose with defaults and override
trait Alarm[a] = {
  fn ring!(x: a) -> Str = "default"
}

impl Alarm[Int] = {
  override fn ring!(x) = "ring:" + Show.show(x)
}

puts(Alarm.ring!(1))  # output: ring:1
