# VN46: Generic default trait methods specialize and codegen through calls
trait Mappable[a] = {
  fn map[b](x: a, f: (a) -> b) -> b = f(x)
}

impl Mappable[Int] = {}

puts(Mappable.map(1, (n: Int) -> Show.show(n)))  # output: 1
puts(Mappable.map(1, (n: Int) -> n + 41))    # output: 42
