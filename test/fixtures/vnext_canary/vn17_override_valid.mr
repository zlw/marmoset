# VN17: Override keyword correctly replaces a trait default method
trait Greeter[a] = {
  fn greet(x: a) -> Str = "hello"
}
impl Greeter[Int] = {
  override fn greet(x: Int) -> Str = "hi"
}
puts(Greeter.greet(1))  # output: hi
