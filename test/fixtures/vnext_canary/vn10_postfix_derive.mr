# VN10: Postfix derive on constructor-bearing type definition
type Color = {
  Red,
  Green,
  Blue,
} derive Eq, Show
let c = Color.Red
puts(Show.show(c))    # output: Red
puts(Eq.eq(c, Color.Red))   # output: true
puts(Eq.eq(c, Color.Blue))  # output: false
