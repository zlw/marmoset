# VN73: Record-pattern case arms may use block bodies
type Person = { name: Str, age: Int }

fn describe(r: Person) -> Str = match r {
  case { name:, age: }: {
    let prefix = name
    prefix + ":" + age.show()
  }
  case _: "other"
}

puts(describe({ name: "mia", age: 7 }))  # output: mia:7
