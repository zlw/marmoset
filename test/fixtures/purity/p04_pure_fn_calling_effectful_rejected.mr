# P4: Pure function calling an effectful function should be caught
fn side_effect(x: Int) => Int = {
  puts(x)
  x
}
fn pure_caller(y: Int) -> Int = {  # error: effectful
  side_effect(y)
}
pure_caller(1)
