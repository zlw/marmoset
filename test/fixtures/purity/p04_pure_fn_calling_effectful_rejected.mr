# P4: Pure function calling an effectful function should be caught
let side_effect = fn(x: int) => int {
  puts(x)
  x
}
let pure_caller = fn(y: int) -> int {  # error: effectful
  side_effect(y)
}
pure_caller(1)
