# P24: BUG - let-bound pure function type annotation still accepts an effectful lambda
# EXPECTED (ideal): Should reject
# ACTUAL: Builds successfully under vNext syntax too
let f: (Int) -> Int = (x: Int) => {
  puts(x)
  x
}
f(1)
