# P26: Pure fn with effectful qualified call is rejected
type Logger = { prefix: Str }

impl Logger = {
  fn log(l: Logger, msg: Str) => Str = {
    puts(msg)
    msg
  }
}

fn bad(l: Logger) -> Str = {  # error: effectful
  Logger.log(l, "x")
}

bad({prefix: "p"})
