# P17: Pure fn with effectful call in if-branch is rejected
fn log(x: Int) => Int = {
  puts(x)
  x
}
fn maybe_log(flag: Bool, x: Int) -> Int = {  # error: effectful
  if (flag) { log(x) } else { x }
}
maybe_log(true, 42)
