# P18: Pure fn with effectful call in match-arm is rejected
enum Choice = {
  Yes,
  No,
}
fn log(x: Int) => Int = {
  puts(x)
  x
}
fn decide(c: Choice, x: Int) -> Int = {  # error: effectful
  match c {
    case Choice.Yes: log(x)
    case Choice.No: x
  }
}
decide(Choice.Yes, 1)
