# CS62: K69: is-check then match in same function body
fn f(x: Int | Str) -> Int = {
  let base = if (x is Int) { x } else { 0 }
  match base {
    case 0: 0
    case _: base + 10
  }
}
puts(f(2))  # output: 12
