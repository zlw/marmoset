# CS61: K68: effectful fn with if+match+puts
fn f(flag: Bool, n: Int) => Int = {
  if (flag) {
    puts("branch_a")
    match n { case 1: 99 case _: 0 }
  } else {
    puts("branch_b")
    0
  }
}
# output: branch_a
# output: 99
puts(f(true, 1))
