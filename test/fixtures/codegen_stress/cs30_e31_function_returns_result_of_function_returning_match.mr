# CS30: E31: function returns result of function returning match
fn inner(x: Int) -> Int = {
  match x { case 1: 100 case 2: 200 case 3: 300 case _: 0 }
}
fn outer(x: Int) -> Int = { inner(x) }
puts(outer(3))  # output: 300
