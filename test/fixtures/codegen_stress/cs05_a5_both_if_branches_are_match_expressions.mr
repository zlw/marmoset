# CS05: A5: both if-branches are match expressions
fn f(flag: Bool, n: Int) -> Int = {
  if (flag) {
    match n { case 1: 100 case 2: 200 case _: 0 }
  } else {
    match n { case 1: 1000 case 2: 2000 case _: 0 }
  }
}
puts(f(true, 2))  # output: 200
