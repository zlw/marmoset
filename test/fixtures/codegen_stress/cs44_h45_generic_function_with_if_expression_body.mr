# CS44: H45: generic function with if-expression body
fn process(x, flag: Bool) = {
  if (flag) { x } else { x }
}
puts(process(10, true))  # output: 10
puts(process("hello", false))  # output: hello
