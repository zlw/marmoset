# CS27: D27: former parser limit, let in match arm block now supported
fn f(x: Int) => Int = {
  match x {
    case 1: {
      let v = 100
      puts(v)  # output: 100
      v
    }
    case _: 0
  }
}
f(1)
