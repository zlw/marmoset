# CS27: D27: [PARSER-LIMIT] let in match arm block is rejected
let f = fn(x: int) => int {
  match x {
    1: {
      let v = 100  # error: parse-unexpected-token
      puts(v)
      v
    }
    _: 0
  }
}
f(1)
