# CS10: B10: [BUG-CAPTURE] closure captures outer var, uses in match
let offset = 40
fn f(x: Int) -> Int = {
  match x {
    case 1: offset + 1
    case 2: offset + 2
    case _: offset
  }
}
puts(f(2))  # output: 42
