# CS58: H59: record spread then match on field
let base = { x: 10, y: 20 }
let extended = { ...base, x: 100 }
let result = match extended.x {
  case 100: 100
  case _: 0
}
puts(result)  # output: 100
