# CS07: A7: ternary chain (nested if in both branches)
fn f(a: Bool, b: Bool, c: Bool) -> Int = {
  if (a) {
    if (b) { 1 } else { 2 }
  } else {
    if (c) { 3 } else { 4 }
  }
}
puts(f(false, true, false))  # output: 4
