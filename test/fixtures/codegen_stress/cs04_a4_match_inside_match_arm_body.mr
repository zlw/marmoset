# CS04: A4: match inside match arm body
fn f(x: Int, y: Int) -> Str = {
  match x {
    case 1: match y {
      case 1: "inner1"
      case 2: "inner2"
      case _: "inner_other"
    }
    case _: "outer"
  }
}
puts(f(1, 2))  # output: inner2
