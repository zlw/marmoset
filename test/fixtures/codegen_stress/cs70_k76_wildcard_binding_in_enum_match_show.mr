# CS70: K76: wildcard binding in enum match + show
enum Option[a] = { Some(a), None }
fn f(opt: Option[Int]) -> Str = {
  match opt {
    case Option.Some(v): v.show()
    case Option.None: "none"
  }
}
puts(f(Option.Some(99)))  # output: 99
