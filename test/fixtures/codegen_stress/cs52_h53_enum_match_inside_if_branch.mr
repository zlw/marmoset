# CS52: H53: enum match inside if-branch
enum Option[a] = { Some(a), None }
fn f(flag: Bool, opt: Option[Int]) -> Int = {
  if (flag) {
    match opt {
      case Option.Some(v): v
      case Option.None: 0
    }
  } else {
    0
  }
}
puts(f(true, Option.Some(42)))  # output: 42
