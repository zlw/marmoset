# CS35: F36: multiple is-checks in sequence
let x: Int | Str = 42
let y: Int | Str = "hello"
let a = if (x is Int) { x } else { 0 }
let b = if (y is Str) { y } else { "none" }
puts(a)  # output: 42
puts(b)  # output: hello
