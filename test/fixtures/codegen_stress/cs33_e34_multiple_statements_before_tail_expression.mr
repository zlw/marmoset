# CS33: E34: multiple statements before tail expression
fn compute(a: Int, b: Int) -> Int = {
  let sum = a + b
  let doubled = sum * 2
  let halved = doubled / 2
  halved + 10
}
puts(compute(2, 3))  # output: 15
