# CS56: H57: if-match-if combined chain
fn f(flag: Bool, n: Int, check: Bool) -> Int = {
  if (flag) {
    match n {
      case 1: if (check) { 99 } else { 0 }
      case _: 50
    }
  } else {
    0
  }
}
puts(f(true, 1, true))  # output: 99
