# CS46: H47: recursive fn with match and if combined
fn fib(n: Int) -> Int = {
  match n {
    case 0: 0
    case 1: 1
    case _: if (n > 1) { fib(n - 1) + fib(n - 2) } else { 0 }
  }
}
puts(fib(10))  # output: 55
