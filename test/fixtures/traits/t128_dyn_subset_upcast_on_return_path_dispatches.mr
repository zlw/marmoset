# T128: Returning a Dyn subset from a wider Dyn local keeps callable witnesses
fn shrink(flag: Bool) -> Dyn[Show] = if (flag) {
  let wide: Dyn[Show & Eq] = 42
  wide
} else {
  "no"
}

puts(Show.show(shrink(true)))   # output: 42
puts(Show.show(shrink(false)))  # output: no
