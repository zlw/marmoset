# T25: Field-only trait conflicting supertrait fields are rejected
trait has_int {  # error: conflicting types across supertraits
  x: int
}
trait has_string {
  x: string
}
trait bad: has_int + has_string {
}
let x: bad = { x: 1 }
