# T25: Legacy field-style supertrait syntax is rejected before conflict checking
trait HasInt = {
  x: Int  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
trait HasString = {
  x: Str
}
trait Bad: HasInt & HasString = {}
