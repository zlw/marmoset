# T25: Field-only trait conflicting supertrait fields are rejected
trait HasInt = {
  x: Int
}
trait HasString = {
  x: Str
}
trait Bad: HasInt & HasString = {}  # error: conflicting types across supertraits
