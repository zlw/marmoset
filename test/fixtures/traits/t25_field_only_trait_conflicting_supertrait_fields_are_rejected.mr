# T25: Field-only trait conflicting supertrait fields are rejected
trait HasInt = {  # error: conflicting types across supertraits
  x: Int
}
trait HasString = {
  x: Str
}
trait Bad: HasInt & HasString = {}
let x: Bad = { x: 1 }
