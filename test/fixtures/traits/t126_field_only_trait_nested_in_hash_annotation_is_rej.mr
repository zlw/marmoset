# T126: Legacy field-style trait syntax is rejected in hash annotations
trait Named = {
  name: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}

let by_name: Hash[Str, Named] = {}
by_name
