# T112: Trait-typed return value inferred at call site
trait Named = {
  name: Str  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
fn extract(x: Named) -> Str = x.name
let result = extract({ name: "alice", age: 30, role: "admin" })
puts(result)  # output: alice
