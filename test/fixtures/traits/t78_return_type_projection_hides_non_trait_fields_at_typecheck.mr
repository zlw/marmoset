# T78: Return type projection hides non-trait fields at typecheck
trait Named = {
  name: Str
}
fn mk_named(n: Str, a: Int) -> Named = { name: n, age: a }  # error: cannot be used as a type in vNext
let x = mk_named("alice", 30)
puts(x.age)
