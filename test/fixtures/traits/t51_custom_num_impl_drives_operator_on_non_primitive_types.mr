# T51: Custom num impl drives + operator on non-primitive types
# output: 13
enum boxed {
  val(int)
}
impl num for boxed {
  fn add(x: boxed, y: boxed) -> boxed { x }
  fn sub(x: boxed, y: boxed) -> boxed { x }
  fn mul(x: boxed, y: boxed) -> boxed { x }
  fn div(x: boxed, y: boxed) -> boxed { x }
}
let a = boxed.val(13)
let b = boxed.val(2)
match (a + b) {
  boxed.val(n): puts(n)
}
