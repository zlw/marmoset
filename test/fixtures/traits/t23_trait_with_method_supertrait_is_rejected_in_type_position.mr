# T23: Trait with method supertrait is rejected in type position
trait Named = {
  name: Str
}
trait Show[a] = {
  fn show(x: a) -> Str
}
trait NamedShow: Named & Show = {}
let x: NamedShow = { name: "alice" }  # error: cannot be used as a type in vNext
