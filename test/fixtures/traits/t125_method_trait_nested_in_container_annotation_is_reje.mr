# T125: Method trait nested in a container annotation is rejected
trait Renderable[a] = {
  fn render(x: a) -> Str
}

let xs: List[Renderable] = []  # error: cannot be used as a type; use a constrained type parameter or Dyn[...] instead
xs
