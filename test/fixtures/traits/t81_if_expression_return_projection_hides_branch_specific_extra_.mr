# T81: If-expression return projection hides branch-specific extra fields
trait Named = {
  name: Str
}
let x: Named = if (true) { { name: "branch_a", extra1: 1 } } else { { name: "branch_b", extra2: "two" } }  # error: cannot be used as a type in vNext
puts(x.extra1)
