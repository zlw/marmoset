# T107: Legacy field-style trait syntax is rejected in aliased type position
trait Named = {
  name: Str  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
type Nameable = Named
let x: Nameable = { name: "alice", age: 30 }
puts(x.age)
