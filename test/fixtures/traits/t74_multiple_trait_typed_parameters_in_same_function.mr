# T74: Multiple trait-typed parameters in same function
# output: alice bob
trait named {
  name: string
}
let greet_both = fn(a: named, b: named) -> string {
  a.name + " " + b.name
}
puts(greet_both({ name: "alice", age: 30 }, { name: "bob", role: "admin" }))
