# T89: Impl block for field-only trait with alias type is rejected
trait Named = {  # error: field-only and cannot have impl blocks
  name: Str
}
type Person = { name: Str, age: Int }
impl Named[Person] = {}
puts(1)
