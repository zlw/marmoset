# T89: Legacy field-style trait impl syntax is rejected for alias targets
trait Named = {
  name: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
type Person = { name: Str, age: Int }
impl Named[Person] = {}
puts(1)
