# T89: Impl block for field-only trait with alias type is rejected
# error: field-only and cannot have impl blocks
trait named {
  name: string
}
type person = { name: string, age: int }
impl named for person {
}
puts(1)
