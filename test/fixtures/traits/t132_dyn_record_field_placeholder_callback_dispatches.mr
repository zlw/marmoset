# T132: Dyn trait-object fields package direct values before placeholder callbacks consume them
type Box[a] = { value: a }

fn map_box[a, b](box: Box[a], f: (a) -> b) -> b = f(box.value)

let value: Dyn[Show & Eq] = 42
let box = {value: value}
puts(Show.show(map_box(box, _)))  # output: 42
