# T36: Duplicate impl for same trait and type fails
trait Show[a] = {  # error: Duplicate impl for trait
  fn show(x: a) -> Str
}
impl Show[Int] = {
  fn show(x: Int) -> Str = "first"
}
impl Show[Int] = {
  fn show(x: Int) -> Str = "second"
}
42
