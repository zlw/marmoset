# T101: Supertrait chain with conflicting field types is rejected
trait has_x_int {  # error: conflicting types across supertraits
  x: int
}
trait has_x_string {
  x: string
}
trait bad_combo: has_x_int + has_x_string {
}
let x: bad_combo = { x: 1 }
