# T101: Supertrait chain with conflicting field types is rejected
trait HasXInt = {
  x: Int
}
trait HasXString = {
  x: Str
}
trait BadCombo: HasXInt & HasXString = {}  # error: conflicting types across supertraits
