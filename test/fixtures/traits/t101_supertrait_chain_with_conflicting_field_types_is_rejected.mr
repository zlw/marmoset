# T101: Supertrait chain with conflicting field types is rejected
trait HasXInt = {
  x: Int  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
trait HasXString = {
  x: Str
}
trait BadCombo: HasXInt & HasXString = {}
