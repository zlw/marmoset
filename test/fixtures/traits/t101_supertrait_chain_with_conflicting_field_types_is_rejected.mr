# xfail: legacy bare trait-as-type syntax was removed by syntax-rework; method trait objects move to follow-up Dyn syntax
# T101: Supertrait chain with conflicting field types is rejected
trait HasXInt = {  # error: conflicting types across supertraits
  x: Int
}
trait HasXString = {
  x: Str
}
trait BadCombo: HasXInt & HasXString = {}
let x: BadCombo = { x: 1 }
