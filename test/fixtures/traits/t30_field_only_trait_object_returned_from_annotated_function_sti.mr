# T30: Legacy field-style trait syntax is rejected before return projection rules apply
trait Named = {
  name: Str  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
fn mk(age: Int) -> Named = { name: "alice", age: age }
let x = mk(1)
puts(x.age)
