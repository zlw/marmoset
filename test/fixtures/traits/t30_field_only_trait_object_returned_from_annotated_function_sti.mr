# T30: Field-only trait object returned from annotated function still hides non-projected fields
trait Named = {
  name: Str
}
fn mk(age: Int) -> Named = { name: "alice", age: age }  # error: cannot be used as a type in vNext
let x = mk(1)
puts(x.age)
