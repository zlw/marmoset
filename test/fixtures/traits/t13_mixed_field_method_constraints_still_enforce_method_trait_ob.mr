# T13: Mixed field+method constraints still enforce method trait obligations
trait Named = {
  name: Str
}
trait Shown[a] = {
  fn show(x: a) -> Str
}
fn get_name[t: Named & Shown](x: t) -> Str = x.name
let p = { name: "alice" }
puts(get_name(p))  # error: does not implement trait
