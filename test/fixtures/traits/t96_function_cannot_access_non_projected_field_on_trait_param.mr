# T96: Function cannot access non-projected field on trait param
trait Greetable = {
  greeting: Str  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
fn get_id(g: Greetable) -> Int = g.id
puts(get_id({ greeting: "hello", id: 1 }))
