# T96: Function cannot access non-projected field on trait param
trait Greetable = {
  greeting: Str
}
fn get_id(g: Greetable) -> Int = g.id  # error: not guaranteed by constraints
puts(get_id({ greeting: "hello", id: 1 }))
