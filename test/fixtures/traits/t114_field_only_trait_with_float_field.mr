# T114: Legacy field-style trait syntax with Float fields is rejected
trait Measured = {
  value: Float  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
let x: Measured = { value: 3.14, unit: "m" }
puts(x.value)  # output: 3.14
