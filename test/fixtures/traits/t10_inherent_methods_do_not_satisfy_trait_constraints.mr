# T10: Inherent methods do not satisfy trait constraints
type Point = { x: Int }
impl Point = {
  fn show(p: Point) -> Str = "point"
}
fn f[t: Show](x: t) -> Str = Show.show(x)
let p = {x: 1}
puts(f(p))  # error: does not implement trait show
