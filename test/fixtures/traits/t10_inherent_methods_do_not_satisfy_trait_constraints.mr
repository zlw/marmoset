# T10: Inherent methods do not satisfy trait constraints
# error: does not implement trait show
type point = { x: int }
impl point {
  fn show(p: point) -> string {
    "point"
  }
}
let f = fn[t: show](x: t) -> string {
  x.show()
};
let p: point = { x: 1 }
puts(f(p))
