# T24: Legacy generic field-style supertrait syntax is rejected
trait Tagged[a] = {
  tag: a  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
trait TaggedLike: Tagged = {}
