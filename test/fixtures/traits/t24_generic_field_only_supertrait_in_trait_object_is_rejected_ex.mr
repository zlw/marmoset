# T24: Generic field-only supertrait in trait object is rejected explicitly
# error: generic field-only supertrait 'tagged' is not supported
trait tagged[a] {
  tag: a
}
trait tagged_like: tagged {
}
let x: tagged_like = { tag: 1 }
