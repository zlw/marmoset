# xfail: legacy bare trait-as-type syntax was removed by syntax-rework; method trait objects move to follow-up Dyn syntax
# T24: Generic field-only supertrait in trait object is rejected explicitly
trait Tagged[a] = {  # error: generic field-only supertrait
  tag: a
}
trait TaggedLike: Tagged = {}
let x: TaggedLike = { tag: 1 }
