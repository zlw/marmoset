# T24: Legacy generic field-style supertrait syntax is rejected
trait Tagged[a] = {
  tag: a  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
trait TaggedLike: Tagged = {}
