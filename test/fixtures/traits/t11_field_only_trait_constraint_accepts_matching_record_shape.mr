# T11: Field-only trait constraint accepts matching record shape
trait Named = {
  name: Str
}
fn get_name[t: Named](x: t) -> Str = x.name
let person = { name: "alice", age: 42 }
puts(get_name(person))  # output: alice
