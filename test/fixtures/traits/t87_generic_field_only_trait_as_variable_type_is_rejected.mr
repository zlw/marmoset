# T87: Generic field-only trait as variable type is rejected
trait container[a] {  # error: generic field-only trait objects are not supported in this phase
  value: a
}
let x: container = { value: 42 }
