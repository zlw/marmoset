# T105: Legacy field-style trait syntax is rejected before Dyn method-dispatch rules apply
trait Named = {
  name: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
let x: Named = { name: "alice" }
puts(Show.show(x))
