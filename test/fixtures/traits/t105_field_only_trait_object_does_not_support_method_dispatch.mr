# xfail: bare trait-as-type semantics are deferred to syntax-rework-followup Dyn syntax
# T105: Field-only trait object does not support method dispatch
trait Named = {
  name: Str
}
let x: Named = { name: "alice" }
puts(x.show())  # error: No method 'show' found for type
