# T17: Legacy field-style trait syntax is rejected in type position
trait Named = {
  name: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
let x: Named = { name: "alice", age: 42 }
puts(x.name)  # output: alice
