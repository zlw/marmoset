# xfail: bare trait-as-type semantics are deferred to syntax-rework-followup Dyn syntax
# T17: Field-only trait as type projects records to required shape
trait Named = {
  name: Str
}
let x: Named = { name: "alice", age: 42 }
puts(x.name)  # output: alice
