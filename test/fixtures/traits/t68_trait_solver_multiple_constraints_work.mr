# T68: Trait solver: multiple constraints work
fn show_if_equal[a: Show & Eq](x: a, y: a) -> Str = {
  if (x.eq(y)) {
    x.show()
  } else {
    "different"
  }
}
let result = show_if_equal(42, 42)
puts(result)  # output: 42
