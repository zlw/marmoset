# T84: Method-only trait as variable type is rejected
trait Renderable[a] = {
  fn render(x: a) -> Str
}
let x: Renderable = 42  # error: cannot be used as a type; use a constrained type parameter or Dyn[...] instead
