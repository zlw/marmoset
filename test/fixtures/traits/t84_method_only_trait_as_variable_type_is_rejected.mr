# xfail: bare trait-as-type semantics are deferred to syntax-rework-followup Dyn syntax
# T84: Method-only trait as variable type is rejected
trait Renderable[a] = {  # error: method and mixed trait objects are not supported in this phase
  fn render(x: a) -> Str
}
let x: Renderable = 42
