# T85: BUG-PARAM-TYPE-TRAIT-KIND: method-only trait param uses shorthand constraint
trait Renderable[a] = {
  fn render(x: a) -> Str
}
impl Renderable[Int] = {
  fn render(x: Int) -> Str = "ok"
}
fn f(x: Renderable) -> Str = Renderable.render(x)
puts(f(42))  # output: ok
