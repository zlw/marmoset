# T85: BUG-PARAM-TYPE-TRAIT-KIND: method-only trait as param type should fail
trait renderable[a] {  # error: method and mixed trait objects are not supported in this phase
  fn render(x: a) -> string
}
let f = fn(x: renderable) -> string {
  "nope"
}
puts(f(42))
