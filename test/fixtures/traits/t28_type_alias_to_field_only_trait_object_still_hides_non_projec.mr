# T28: Type-alias to field-only trait object still hides non-projected fields
# error: Record field 'age' not found in type
trait named {
  name: string
}
type named_alias = named
let x: named_alias = { name: "alice", age: 1 }
puts(x.age)
