# T28: Legacy field-style trait syntax is rejected in aliased type position
trait Named = {
  name: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
type NameAlias = Named
let x: NameAlias = { name: "alice", age: 1 }
puts(x.age)
