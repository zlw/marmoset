# T28: Type-alias to field-only trait object still hides non-projected fields
trait Named = {
  name: Str
}
type NameAlias = Named
let x: NameAlias = { name: "alice", age: 1 }
puts(x.age)  # error: Record field 'age' not found in type
