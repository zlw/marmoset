# T34: Impl with multiple methods compiles (smoke)
trait Eq[a] = {
  fn eq(x: a, y: a) -> Bool
  fn ne(x: a, y: a) -> Bool
}
impl Eq[Int] = {
  fn eq(x: Int, y: Int) -> Bool = true
  fn ne(x: Int, y: Int) -> Bool = false
}
42
