# T86: Legacy mixed field+method trait syntax is rejected in type position
trait Displayable[a] = {
  name: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
  fn display(x: a) -> Str
}
let x: Displayable = { name: "alice" }
