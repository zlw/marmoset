# xfail: bare trait-as-type semantics are deferred to syntax-rework-followup Dyn syntax
# T86: Mixed trait (fields + methods) as type is rejected
trait Displayable[a] = {  # error: method and mixed trait objects are not supported in this phase
  name: Str
  fn display(x: a) -> Str
}
let x: Displayable = { name: "alice" }
