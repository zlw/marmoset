# T86: Mixed trait (fields + methods) as type is rejected
trait Displayable[a] = {
  name: Str
  fn display(x: a) -> Str
}
let x: Displayable = { name: "alice" }  # error: cannot be used as a type in vNext
