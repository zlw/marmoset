# T69: Trait solver: multiple constraints with different values
fn show_if_equal[a: Show & Eq](x: a, y: a) -> Str = {
  if (Eq.eq(x, y)) {
    Show.show(x)
  } else {
    "different"
  }
}
let result = show_if_equal(42, 99)
puts(result)  # output: different
