# T19: Field-only trait object includes inherited fields from supertraits
trait Named = {
  name: Str
}
trait Aged = {
  age: Int
}
trait Person: Named & Aged = {}
let p: Person = { name: "alice", age: 43, active: true }  # error: cannot be used as a type in vNext
puts(p.age)  # output: 43
