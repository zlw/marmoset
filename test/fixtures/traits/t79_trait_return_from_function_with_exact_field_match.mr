# T79: Trait return from function with exact field match
trait Named = {
  name: Str
}
fn mk() -> Named = { name: "alice" }  # error: cannot be used as a type in vNext
let x = mk()
puts(x.name)  # output: alice
