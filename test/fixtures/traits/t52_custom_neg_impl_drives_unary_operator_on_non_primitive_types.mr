# T52: Custom neg impl drives unary - operator on non-primitive types
enum boxed {
  val(int)
}
impl neg for boxed {
  fn neg(x: boxed) -> boxed { x }
}
let a = boxed.val(5)
match (-a) {  # output: 5
  boxed.val(n): puts(n)
}
