# T52: Custom Neg impl drives unary - operator on non-primitive types
enum Boxed = {
  Val(Int),
}
impl Neg[Boxed] = {
  fn neg(x: Boxed) -> Boxed = x
}
let a = Boxed.Val(5)
match (-a) {  # output: 5
  case Boxed.Val(n): puts(n)
}
