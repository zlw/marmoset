# T113: Legacy field-style trait syntax with Bool fields is rejected
trait Togglable = {
  active: Bool  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
let x: Togglable = { active: true, name: "switch" }
puts(x.active)  # output: true
