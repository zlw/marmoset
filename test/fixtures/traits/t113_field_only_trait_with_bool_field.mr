# T113: Field-only trait with Bool field
trait Togglable = {
  active: Bool
}
let x: Togglable = { active: true, name: "switch" }  # error: cannot be used as a type in vNext
puts(x.active)  # output: true
