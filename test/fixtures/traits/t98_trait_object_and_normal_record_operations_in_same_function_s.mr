# T98: Trait object and normal record operations in same function scope
trait Named = {
  name: Str
}
fn process(x: Named, extra: Int) -> Str = {
  let n = x.name
  let plain = { value: extra }
  n + " " + plain.value.show()
}
puts(process({ name: "alice", age: 30 }, 42))  # output: alice 42
