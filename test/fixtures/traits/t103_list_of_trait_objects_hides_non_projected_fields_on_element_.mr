# T103: List of trait objects hides non-projected fields on element access
trait Named = {
  name: Str
}
let a: Named = { name: "alice", role: "admin" }  # error: cannot be used as a type in vNext
let xs: List[Named] = [a]
puts(xs[0].role)
