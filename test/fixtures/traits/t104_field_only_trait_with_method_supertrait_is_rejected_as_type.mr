# T104: Field-only trait with method supertrait is rejected as type
trait Show[a] = {
  fn show(x: a) -> Str
}
trait Describable: Show = {
  label: Str
}
let x: Describable = { label: "hi" }  # error: cannot be used as a type in vNext
