# T104: Legacy field-style trait syntax with method supertraits is rejected in type position
trait Show[a] = {
  fn show(x: a) -> Str
}
trait Describable: Show = {
  label: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
let x: Describable = { label: "hi" }
