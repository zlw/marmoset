# T67: Trait solver: array lacks show (should fail typecheck)
let check = fn[a: show](x: a) -> string {  # error: does not implement trait
  x.show()
}
let arr = [1, 2, 3]
let result = check(arr)
puts(result)
