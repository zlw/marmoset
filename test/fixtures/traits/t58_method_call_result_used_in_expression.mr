# T58: Method call result used in expression
trait Double[a] = {
  fn double(x: a) -> Int
}
impl Double[Int] = {
  fn double(x: Int) -> Int = x + x
}
let n = 21
let result = n.double()
puts(result)  # output: 42
