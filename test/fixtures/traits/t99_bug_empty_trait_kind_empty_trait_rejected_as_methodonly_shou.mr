# xfail: bare trait-as-type semantics are deferred to syntax-rework-followup Dyn syntax
# T99: BUG-EMPTY-TRAIT-KIND: empty trait rejected as MethodOnly (should be FieldOnly)
trait Marker = {  # error: method and mixed trait objects are not supported
}
let x: Marker = { anything: 1, goes: "here" }
puts("ok")
