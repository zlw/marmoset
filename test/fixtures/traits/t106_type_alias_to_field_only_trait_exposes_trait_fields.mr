# T106: Legacy field-style trait syntax is rejected in aliased type position
trait Named = {
  name: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
type Nameable = Named
let x: Nameable = { name: "alice", age: 30 }
puts(x.name)  # output: alice
