# T106: Type alias to field-only trait exposes trait fields
trait Named = {
  name: Str
}
type Nameable = Named  # error: cannot be used as a type in vNext
let x: Nameable = { name: "alice", age: 30 }
puts(x.name)  # output: alice
