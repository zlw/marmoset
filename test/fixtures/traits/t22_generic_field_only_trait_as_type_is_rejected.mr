# xfail: bare trait-as-type semantics are deferred to syntax-rework-followup Dyn syntax
# T22: Generic field-only trait as type is rejected
trait Tagged[a] = {  # error: generic field-only trait objects are not supported in this phase
  tag: a
}
let x: Tagged = { tag: 1 }
