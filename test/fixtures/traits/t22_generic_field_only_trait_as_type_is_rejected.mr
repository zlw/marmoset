# T22: Legacy generic field-style trait syntax is rejected in type position
trait Tagged[a] = {
  tag: a  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
let x: Tagged = { tag: 1 }
