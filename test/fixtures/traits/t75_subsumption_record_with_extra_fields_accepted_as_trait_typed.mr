# T75: Subsumption: record with extra fields accepted as trait-typed param
trait Greeter = {
  greeting: Str  # error: Trait bodies are method-only; move structural fields to a shape declaration
}
fn say(g: Greeter) -> Str = g.greeting
puts(say({ greeting: "world", volume: 11, language: "en" }))  # output: world
