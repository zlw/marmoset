# T45: Equality requires eq trait (functions should fail)
let same = fn[a](x: a, y: a) -> bool {  # error: missing-impl
  x == y
};
let id1 = fn(n: int) -> int { n };
let id2 = fn(n: int) -> int { n + 1 };
same(id1, id2)
