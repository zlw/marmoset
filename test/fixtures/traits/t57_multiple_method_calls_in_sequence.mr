# T57: Multiple method calls in sequence
trait Show[a] = {
  fn show(x: a) -> Str
}
impl Show[Int] = {
  fn show(x: Int) -> Str = "number"
}
let x = 1
let y = 2
puts(Show.show(x))  # output: number
puts(Show.show(y))  # output: number
