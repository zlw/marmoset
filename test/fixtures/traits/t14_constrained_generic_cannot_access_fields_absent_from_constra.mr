# T14: Constrained generic cannot access fields absent from constraints
trait Named = {
  name: Str
}
fn get_age[t: Named](x: t) -> Int = x.age  # error: not guaranteed by constraints
let p = { name: "alice", age: 42 }
puts(get_age(p))
