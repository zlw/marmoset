# T88: Legacy generic field-style trait syntax is rejected before shorthand-constraint checking
trait Container[a] = {
  value: a  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
fn f(x: Container) -> Int = x.value
puts(f({ value: 42 }))  # output: 42
