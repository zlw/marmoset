# T88: BUG-PARAM-TYPE-TRAIT-KIND: generic field-only trait param uses shorthand constraint
trait Container[a] = {
  value: a
}
fn f(x: Container) -> Int = x.value
puts(f({ value: 42 }))  # output: 42
