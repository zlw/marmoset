# T21: Mixed trait as type is rejected
trait named_show[a] {  # error: method and mixed trait objects are not supported in this phase
  name: string
  fn show(x: a) -> string
}
let x: named_show = { name: "alice" }
