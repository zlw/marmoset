# T21: Mixed trait as type is rejected
trait NamedShow[a] = {  # error: method and mixed trait objects are not supported in this phase
  name: Str
  fn show(x: a) -> Str
}
let x: NamedShow = { name: "alice" }
