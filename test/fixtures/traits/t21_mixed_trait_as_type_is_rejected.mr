# T21: Legacy mixed field+method trait syntax is rejected in type position
trait NamedShow[a] = {
  name: Str  # error: Trait bodies are method-only; move structural fields to a shape declaration
  fn show(x: a) -> Str
}
let x: NamedShow = { name: "alice" }
