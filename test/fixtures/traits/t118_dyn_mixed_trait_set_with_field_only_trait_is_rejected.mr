# T118: Dyn rejects legacy trait sets that include field-style traits
trait Named = {
  name: Str  # error: Trait bodies are method-only; use 'shape' for structural field contracts
}
let x: Dyn[Show & Named] = 42
x
