# T115: Two different trait-typed params in one function
# output: alice 42
trait named {
  name: string
}
trait scored {
  score: int
}
let describe = fn(n: named, s: scored) -> string {
  n.name + " " + s.score.show()
}
puts(describe({ name: "alice", id: 1 }, { score: 42, rank: "A" }))
