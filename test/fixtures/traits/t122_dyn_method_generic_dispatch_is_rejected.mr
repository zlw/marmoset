# T122: Dyn rejects method-generic dispatch
trait Caster[a] = {
  fn cast[b](x: a, f: (a) -> b) -> b
}
impl Caster[Int] = {
  fn cast[b](x: Int, f: (Int) -> b) -> b = f(x)
}
let x: Dyn[Caster] = 1
Caster.cast[Str](x, (n: Int) -> Show.show(n))  # error: method-generic dispatch
