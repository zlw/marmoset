# T55: Impl method body can return direct record literal
type Box = { value: Int }
trait Wrap[a] = {
  fn wrap(x: a) -> Box
}
impl Wrap[Int] = {
  fn wrap(x: Int) -> Box = {value: x}
}
let b = Wrap.wrap(5)
puts(b.value)  # output: 5
