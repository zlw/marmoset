# T90: Supertrait chain inherits parent fields
trait Named = {
  name: Str
}
trait Described: Named = {
  description: Str
}
let x: Described = { name: "alice", description: "a person", hobby: "chess" }  # error: cannot be used as a type in vNext
puts(x.name)  # output: alice
