# T50: Custom Eq impl drives == operator on non-primitive types
type Point = { x: Int }
impl Eq[Point] = {
  fn eq(x: Point, y: Point) -> Bool = false
}
let p1 = {x: 1}
let p2 = {x: 1}
puts(p1 == p2)  # output: false
