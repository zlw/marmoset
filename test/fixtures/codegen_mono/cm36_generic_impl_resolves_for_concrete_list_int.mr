# CM36: Generic impl resolves for concrete list[int]
# output: ok
trait show[a] {
  fn show(x: a) -> string
}
impl show[b: show] for list[b] {
  fn show(x: list[b]) -> string {
    "ok"
  }
}
puts([1, 2, 3].show())
