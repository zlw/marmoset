# CM27: [BUG-3] apply with different callback types
fn apply(f, x) = f(x)
fn inc(n: Int) -> Int = n + 1
fn bang(s: Str) -> Str = s + "!"
puts(apply(inc, 42))  # output: 43
puts(apply(bang, "hello"))  # output: hello!
