# CM09: Poly fn wrapping int in generic enum
enum option[a] { some(a) none }
let wrap = fn(x) { option.some(x) }
let v = wrap(42)
match v {  # output: 42
  option.some(n): puts(n)
  option.none: puts(0)
}
