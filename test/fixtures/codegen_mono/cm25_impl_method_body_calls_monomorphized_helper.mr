# CM25: Impl method body calls monomorphized helper
fn fmt_val(s: Str, n: Int) -> Str = {
  s + ":" + n.show()
}
trait Describe[a] = {
  fn describe(x: a) -> Str
}
impl Describe[Int] = {
  fn describe(x: Int) -> Str = fmt_val("added", x)
}
puts(42.describe())  # output: added:42
