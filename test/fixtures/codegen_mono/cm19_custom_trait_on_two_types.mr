# CM19: Custom trait on two types
trait Label[a] = {
  fn label(x: a) -> Str
}
impl Label[Int] = {
  fn label(x: Int) -> Str = "INT"
}
impl Label[Str] = {
  fn label(x: Str) -> Str = "STR"
}
puts(Label.label(1))  # output: INT
puts(Label.label("a"))  # output: STR
