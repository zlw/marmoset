# CM08: Chain of different typed fns composes correctly
let add1 = fn(x: int) -> int { x + 1 }
let double = fn(x: int) -> int { x * 2 }
let negate = fn(x: int) -> int { 0 - x }
puts(negate(negate(add1(double(20)))))  # output: 41
