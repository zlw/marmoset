# CM10: Poly fn wrapping Str in generic enum
enum Option[a] = { Some(a), None }
fn wrap(x) = Option.Some(x)
let v = wrap("hello")
match v {  # output: hello
  case Option.Some(s): puts(s)
  case Option.None: puts("none")
}
