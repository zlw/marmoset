# CM10: Poly fn wrapping string in generic enum
enum option[a] { some(a) none }
let wrap = fn(x) { option.some(x) }
let v = wrap("hello")
match v {  # output: hello
  option.some(s): puts(s)
  option.none: puts("none")
}
